# AGENTS.md — 内镜中心 VR 监督检查项目

适用于整个工作区；[app/AGENTS.md](app/AGENTS.md) 仅补充 Unity 实现规则。

## 1. 工作流与文档

- 按用户当前要求推进，保留已有未提交修改。只读本次任务相关资料，先核对现状，避免重复实现或制作资源。
- 需求依据：[完整规格](docs/FULL_SCRIPT_SPEC_20260920.md)、[内容矩阵](docs/FULL_SCRIPT_CONTENT_MATRIX.md)。任务与勾选状态只在[合并计划](docs/STARTUP_FLOW_AUDIT_PLAN_20260923.md)维护；其他文档引用它。
- 设计、任务模板及命令见[工作流](docs/WORKFLOW.md)，其他资料见[文档导航](docs/README.md)。进度、错误和验证证据写入计划/报告，不追加为长期规则；历史方案不覆盖现行约定。
- 新需求用 Agent Skills `spec-driven-development`，任务拆解用 `planning-and-task-breakdown`，规则整理用 `context-engineering`；按需要选用，已有明确任务不重走整套流程。访谈仅在用户明确要求时进行。
- 已授权的可逆编辑和必要检查持续完成，不重复确认既有决定。涉及实质性范围取舍或新增授权才询问，并继续不依赖答案的工作。仅要求文档整理时，不顺带实施业务代码。
- 所有执行计划（含对话中的计划）使用 `- [ ]` / `- [x]`。取得所需证据后立即勾选原任务并记录结果；未验证、部分完成或受阻项保持未勾选并说明剩余工作。
- 里程碑只有在全部必需子项和验收条件满足后才勾选。回归时取消勾选并记录原因；取消或替代项注明去向，不算完成。接续旧计划先核对状态，不另建重复清单。

## 2. 产品范围

- 产品为“内镜中心监督检查”VR 培训，覆盖现场观察、资料查阅、依据判断和检查汇总。以0920新剧本为主，旧稿仅补缺；完整内容、资源、视觉和交互均须交付。
- 完整路线：大厅 → 办公室 → 储存库 → 候诊区 → 消化诊疗室 → 呼吸诊疗室 → 清洗消毒室 → 办公室汇总。
- 正式入口只有“开始学习”，由安小卫全程引导；不提供独立核查、模式选择或步行跟随。
- 支持跳过、未答和提交前回查修改；办公室统一提交后只读。跨房保留本次会话记录，应用重启开始新会话，不提供续学存档。
- 内容缺失、主动跳过、未答和答错分别记录，项目缺件不计为学员过失。启动成功、局部测试或目录覆盖不代表完整交付。
- 医疗判断须有适用依据；未知项保留未核实，不编造法规周期、有效期或产品要求。

## 3. 体验者界面

- 只显示当前任务所需的场景、资料、操作和反馈。用自然语言说明检查什么、如何操作及下一步；按钮直接写动作。欢迎页只保留欢迎、简报和开始入口，步骤提示按需出现。
- 开发进度、实现机制、内部编号/路径、测试/版本信息、设计理由和开发提醒只放文档、日志或编辑器工具。该限制同样适用于向导台词、模型标签、字幕及图片内文字；调试按钮不进入 Quest 正式界面。
- 保留简短的模拟身份、来源和真实状态，如“模拟训练记录”“本项暂不可用”“加载失败，请重试”。错误提示给出可行下一步，技术详情写日志，不以隐藏缺口美化完成度。
- 修改文案时检查任务相关性、长度、重复与动作对应；布局需避免遮挡观察对象、保证可读可达。实际画面检查按第7节执行。

## 4. 资源与视觉

- 按0920参考图加工；医疗清单、电子记录和纸质台账保留表格结构、字段关系及载体，不用自绘大卡片/列表替代。
- 本项目模型来源仅限 `C:\Users\i1204\Desktop\Models`；导入前核对源FBX/目录中的实际材质、内嵌或外部贴图、绑定及许可，不得从其他目录补入模型。用户已将模型贴图修正交给另一对话；本对话不自行重贴图、改材质、重绑或重复导入，并在继续模型工作前复核那边的结果。现有资源只读审计后再决定去留，不擅自删除。
- 制作其他视觉资源前查[图册](docs/full-script-visuals/ASSET_CATALOG.md)及已有资源，避免重复制作。
- 允许生图；登记来源/提示词、用途、哈希、实际消费者和验证结果。提示词只描述画面，不把实施计划印入图中；生成图不得冒充医院原表、实拍、检测记录或产品凭证。
- 用户要求生图时，目标是项目实际展示的视觉素材（如剧本中的内镜追溯软件界面、清洗消毒监测记录、设备图或人员图），并接入对应运行内容；不生成展示3D场景的渲染截图，也不把AI生成的界面概念稿或宣传效果图当成项目画面或实现证据。用户另行要求概念稿时，明确标为概念稿，不当作运行资源或验收结果。
- 证明应用画面时，只使用当前工程真实Play或Quest运行时捕获的画面，并注明验证环境与源码状态；不得用概念图、旧截图、单独渲染图或编辑器摆拍替代运行效果。图片整理时先核对图册、规格引用、源文件与实际消费者；清除已过时且无引用/消费者的旧概念图或旧渲染图，同时保留剧本原图、用户参考、许可/来源记录及仍在使用的资源，并同步清单。
- 大厅使用用户提供的360°全景图，正式入口、Unity预览和Quest不恢复高斯模型、渲染或专用MSAA设置；旧PLY/高斯资料仅归档。其他房间保留现有实物模型和业务内容。
- 每项资源需能追溯“源资源 → 运行对象 → 交互/证据 → 画面”；导入不等于接入。缺件保留在矩阵，不用灰盒、空柜、说明页或带教图片冒充成品。
- 竖直悬挂镜体和器械附件须与实际对象相符，不用盘绕胃镜或胃镜本体替代。保留原文件、LICENSE、第三方声明及来源。

## 5. 交互与空间

- 原地坐姿/站姿体验，通过真实手部主动切房和切检查点。Quest仅用面板近触和物件抓取；禁用凝视、准星及鼠标/键盘/手柄射线提交或后备输入。
- 头手保持1:1；观察时环境、面板和对象固定，不随头转或反复回中。面板首次展开可面向用户放在可视可达位置，此后固定；避免多个主面板争抢输入。
- 环境位移/朝向对齐只在遮黑转场内完成，真实追踪未就绪不得用超时伪造成功。
- 向导、环境、操作音效及其他媒体保持静音；用户指定的清洗消毒室水槽视频4:46–5:06片段保留原声。仅该片段为例外，暂停、返回、结束和离房同步管理声音，不开启全局音效。

## 6. 流程隔离与生命周期

- `D:/BotanicalGardenQR-Base` 仅作只读参考，复用合适的美术、向导/对话和触碰能力；不读取或复制其凭据、用户设置和代理配置。
- 新剧本独立拥有正式流程。魔法书、地图召唤、收藏、扫码、旧序章和旧课程前置不进入启动/运行链；不得以创建后隐藏、停止Tick或伪造完成事件代替脱离。历史源码和测试可保留，不反推现行产品要求。
- 仅加载当前房间：停止媒体/交互 → 解绑并销毁旧房对象 → 释放独占资源 → 加载目标。不预加载下一房或全库，不以隐藏代替释放；失败目标清理后再恢复。
- 共有小型UI、向导、手部及会话数据可常驻。

## 7. 编译、测试与验收

- 改动代码、程序集、包依赖或编译相关配置后，必须确认当前工程在 Android 目标下实际导入/编译通过，检查错误日志并修复报错后再交付。静态扫描和历史结果不能代替当前编译；无法验证时明确报告阻碍，不声称可编译、不把已知报错留给用户。
- 编译检查不构建 APK；只补与改动相关的必要局部测试。同一源码/配置状态已有有效结果时不重复运行；纯文档修改不启动 Unity。
- 完整流程和实际画面由用户验收。默认不重跑完整原地流程、不例行启动真实Play、截图或逐房渲染；用户报告 Bug 或明确要求时检查相关路径，定位确有需要再扩大范围，不反复询问是否跑全套。
- 分别报告编译、静态、EditMode、真实Play、渲染和Quest结果。未运行或待用户验收如实记录，不能互相代签；任务验收门槛不因减少检查频率而降低。

## 8. 工程与授权

- 唯一 Unity 工程为 `app/`；本机 `D:/quest3/EndoscopyBuild/app` 是同一目录的联接。所有 Unity 验证启动显式使用 `-buildTarget Android`，不切换至 Standalone/Windows。
- 用户自行构建 APK。未明确要求时，不调用 Build/BuildAndRun 或间接触发构建，不安装、启动或卸载设备应用；计划中的任务不等于设备操作授权。
- 包名 `com.endoscopy.inspection`，显示名“内镜中心监督检查”。不回写旧版本号/versionCode，不关闭必需的 Android Composition Layers 支持。
- 用户明确要求构建时，生成唯一构建标识、递增 versionCode 并记录成功 APK 的 SHA-256。设备核验优先用 `tools/inspect-installed-lesson.ps1`；源码、候选 APK、实际安装包和实测结果分别记录，无设备或哈希不符时明说。
- 完成说明写清改动、实际验证和剩余工作，不把计划、历史通过或局部成功表述为整体完成。
