2026-09-21 最新视觉要求：必须按0920文档参考图加工，尽可能接近现实，不使用自行设计的大卡片界面替代原图记录表格。生图前先读docs/full-script-visuals/ASSET_CATALOG.md；此前生成图已登记，优先复用，新增图必须写入图册、spec/plan与实际接入状态。原模型不能统称白模；先查原材质与贴图绑定，再补缺失表面。办公室已接原模型并开始按原图表格改造，既有木纹/水磨石已绑定派生资源；GI/RESP分室接入验证中。大厅两份PLY已收到，不再要求重复导出或同时生成360。仍手部、静音、固定世界、按房先卸载再加载、重启新进度、用户编译，不构建APK。

2026-09-21 最新资源与加载要求：完整spec/plan已更新v2，优先使用Models.zip已提供模型，能导入先导入，缺失子项明确预留而非计作学员跳过。办公室/诊疗室/电脑/桌子/胃镜已导入ImportedModels，未替换运行灰盒；清洗室与已接P05消毒剂保留。用户明确禁止一口气加载全部模型：仅当前房间按需加载，先关闭媒体/交互、卸载并释放旧房独占资源，再加载目标；不默认预加载下一房间，不把隐藏对象当资源释放。只有大厅请求高斯小样，办公室/诊疗室不重复生成。仍真实手部、静音、固定世界、重启新进度、Android目标及用户自行编译，不构建APK。

2026-09-21 完整剧本目标覆盖：C09是清洗消毒室测试阶段，完整项目按 docs/FULL_SCRIPT_SPEC_20260920.md 和 docs/FULL_SCRIPT_PLAN_20260920.md。新稿为主、旧稿补全，完整路线最终回办公室汇总；实际走到房门后近触切房，房间内仍实际步行和固定世界。带教/独立核查双模式，允许跳过/未答、提交前回查；办公室统一提交后只读。每次应用重启新进度，不做续学存档；场地未定。Visitor已改为大厅启动，实际切房、门口近触、转场恢复与办公室电脑字段阅读已接入；外围房间仍为开发灰盒，正式证据和新稿其余交互未完成，不能宣称完整剧本交付。洗消原六站适当保留，详见 docs/FULL_SCRIPT_INTEGRATION_20260921.md。下方单房间限制只描述旧测试阶段。真实手部、静音、Android和用户自行编译继续有效。

2026-09-20 最新 P01 与开场覆盖：按 docs/P01_SEQUENTIAL_TEACHING_C09.md，魔法书显示发光手印及完整手掌步骤；P01 操作教学/单独练习/显式开始正式观察分开，32 cm 小光圈逐一点亮、判定范围同步缩小。粗黄色进度和百分比提示后续动作；观察后打开对应全景图片教学，等主动继续/回看。持续持镜，最后确认自动取消抓取并收起，不要求松手。覆盖下方旧大框、纯文字讲解、独立图片禁用和最后松手规定；静音、真实手部、固定世界、用户编译不变。

2026-09-20 此前 P01 交互（下列交互流程已被上条覆盖）：按 docs/P01_LIVE_MAGNIFIER_C09.md，入口近触进入全景，实际镜片实时放大 2 倍，稳定观察后展开讲解、近触继续，同一把工具连续三处，最后释放后离站。取消旧抓放翻页及独立特写面板，不重置手中物件。手部、静音、固定世界、用户编译不变。

2026-09-20 最新路线与开场要求：按 docs/VR_ROOM_PATH_BINDING_C09.md，精灵所有地面运动绑定同一房间路线网；开场位置固定，对话留在站点，归线绕行，不跨墙直切。首次可靠头部采样一次性对齐固定入口和初始面向，头手共用校准，之后不重新回中。手部、静音、实际步行、用户编译不变。

2026-09-18 最新覆盖：P01 暂时完全取消答题，使用开场同款小精灵对话和可抓取观察放大镜，三处观察完成/跳过后主动离站。见 [docs/P01_GUIDED_OBSERVATION_C09.md](docs/P01_GUIDED_OBSERVATION_C09.md)。Quest 系统重定义跟踪原点时补偿头手共用 trackingSpace，固定房间坐标，见 [docs/VR_TRACKING_ORIGIN_C09.md](docs/VR_TRACKING_ORIGIN_C09.md)。覆盖旧 P01 图片核查、触碰光圈入口；后五关进一步减少答题仍是设计。全程手部、静音、实际步行、用户自行编译不变。

2026-09-18 最新覆盖：按 docs/VR_HAND_FIXED_C09.md 执行。主线全程只用真实手部：普通面板近触、模型抓取、开场手掌印记；关闭凝视提交、准星和无手凝视后备。房间、全景中心和光圈保持初始世界位置；教学面板约前方 50 cm、眼下 18 cm，展开后固定并正对用户。P01 使用现有参考小精灵对话 prefab 和触碰光圈；P02 改工序排序与现场处置，共 2 项。覆盖本文所有旧凝视/混合输入、引导跟随头部和 P02 图片复用要求。仍静音、实际步行、用户自行编译。

# 本工作区项目约定

2026-09-18 最新后五关范围：用户要求先搭好多媒体框架，允许相关临时素材。P02 图片修复、P03 静音器械视频、P04 毛刷图片与水路证据、P05 网上取得的通用瓶体手部抓取与产品证据、P06 记录对账。见 docs/LATER_STATIONS_C09.md。覆盖旧“后五关不重做”；后五关由 course.json 驱动，答题支持手部近触与共享凝视互斥，可重试、可跳过、无无效退出，结束接主动离站。素材来源与许可须保留；模型不手绘。第一关和静音、VR、步行、用户编译要求继续有效。

实际 Unity 工程是 `D:\quest3\EndoscopyBuild\app`。EndoscopyBuild 为目录联接，不作为另一套工程修改。打开工程必须包含末尾 app。

## 构建与设备

- 用户自行编译。除非明确要求，不编译 APK，不调用 Build/BuildAndRun 或会间接触发 Android 构建的脚本/回调。
- 可以做静态检查与针对性编辑器测试，不将编辑器模拟输入当作真实头显验收。
- 不默认安装、启动或卸载应用。本轮用户已明确授权删除多余 app：已卸载 `com.DefaultCompany.app` 并核验中文 `com.endoscopy.inspection` 保留；这不等于授权安装或启动新版。
- 本项目唯一正式包名为 `com.endoscopy.inspection`，显示名“内镜中心监督检查”。构建前校验身份；显式 Android 构建时生成唯一构建标识和递增 versionCode，成功后写 APK 哈希记录。
- 图形设置/准备工程只能补齐最低版本，不能回写固定旧版本。C08 由 EndoscopyThemeSetup 保持身份和功能设置；保留较新版本和递增 code。
- 当前源码为 0.9.1 / C09；2026-09-17 最近只读核验设备中文包为 0.9.1 / code 23，未核实其对应源码修订及 APK 哈希。本次交互修复尚未部署，不得以同一个显示版本号或本地测试通过声称设备已更新。
- 设备核验优先 `tools/inspect-installed-lesson.ps1`，比较实际 APK SHA-256；无设备时明确失败，不静默使用旧副本当新证据。源码、待装 APK、设备安装版、设备验证结果分别记录。

## 体验与教学

- 当前源码 0.9.1 / C09，方案见 docs/VR_ROOM_C09.md；项目框架由 MR 改为 VR，启动直接进入完整清洗消毒室模型，关闭主线透视、扫描、空间权限和标定入口。
- 主入口仍为模板 Visitor 场景。保留开场邀请、小精灵、导航、对话、问答及主动离站流程，六点和路线按房间过道重排并共用固定坐标。取消图鉴、掌心工具召唤、手动地图和入册收藏步骤；带路时自动显示路线与目标。不要重新引入旧 CircleLesson。
- 玩家实际步行跟随精灵，按钮不传送 XR Origin，无摇杆/WASD。从归档恢复的原房间网格和贴图已放入当前 app 的 EndoscopyTheme/Resources/EndoscopyRoom；只读取归档资产，不恢复旧主线。
- 全程静音。2026-09-17 用户最新要求覆盖旧“面板仅近触”：复用 D:/BotanicalGardenQR-Base 的输入分工，从初遇对话起普通面板统一头向准星停留确认；开场印记及空间物体保留真实手部，开场无可靠手时沿用凝视后备。普通按钮不并列手触入口，不添加鼠标/键盘/手柄射线提交。详见 docs/REFERENCE_INTERACTION_C09.md。
- 三组正确示范与错误修复，修复题数 3/3/4。图片外观不代表实际配置、水路或记录已合规，示意图与案例保留标注和来源。
- 第一站最新教学以 docs/PANORAMA_LESSON_REDESIGN.md 为准：保留 360 全景，转头定位三个观察点，方法示范后使用新情境大图选依据、判断并显式提交。三题替代原文字题；未完成只能重新学习，不能跳过或以提示计分。大图约 0.95 m，展开后固定，提示并入同一界面。公共完成流程消费旧关闭决策，再由现有导航提供主动出发。后五关内容不在本轮重做范围。
- 参考工程只读。保留原 LICENSE 与第三方声明。内部 BotanicalGardenQR 名称和场景 ID 是模板绑定键。
- 最新图片答题覆盖：用户要求答题可用手，第一站图片教学页同时保留真实近触和共享凝视；手部进入操作表面时暂停凝视，退出后清除旧进度。该页约 0.62 m 距离、0.70 m 图宽，等比例缩放维持视角。答错保留选择，提示如何修改并重新提交；不以提示或尝试次数自动计分。
- 编辑器验证不等于头显验收；没有本次设备结果不得声称设备已更新。后续继续只编辑 app。
- 第一站最新引导见 docs/FAIRY_PANORAMA_GUIDE_C09.md：找观察点时用小精灵头像和静音字幕主动提示方向，大角度转头后提示卡回到面前；展开后的图片仍固定。答题内嵌三步操作进度与可展开帮助，不恢复全局 Coach 叠层。
- 后续精简覆盖：教程不常驻，首次答题说明选择后或 8 秒后隐藏，找点对话 8 秒后收起。移除常驻答题教程与三个辅助按钮，只保留两个判断、提交和退出；答错自动提示。详见同一引导文档顶部。
- 用户最新授权允许防卡关“跳过本题”：第一站图片答题增加一个跳过按钮，无须选择或提交即可继续；最后一题跳过可结束本站、主动离站。跳过单独记数，不增加答对数。本条覆盖旧不得跳过及必须全部答对的限制；仍保持手触／凝视与实际步行。
- 最新连续流程：取消第一站找点、图片阅读中的退出按钮，只保留观察、答题／跳过至本站结束、主动出发。方向引导明确目标名、左右及俯仰；教程仍不常驻。覆盖之前保留退出的说明。
