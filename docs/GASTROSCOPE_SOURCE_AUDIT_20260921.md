# 原胃镜悬挂整姿资源核查

## 整姿故障定位与发布拒绝（后续）

按诊断循环，先将原模型失败缩减为一条真实相邻边：两端 `(0.1999512315,0.2505646050,0.0217208862)` 与 `(0.1992798448,0.2500763237,0.0205154419)`，再次运行仍得到653.82倍拉伸。该处不在接头分区阈值上；两端分别被最近中心线算法分配到第524与242段，距路径均约0.114源单位。直接原因是原中心线未覆盖该管段，不能通过放宽边长门槛修复。

新增只读 `tools/trace_scope_surface.py`，沿原面邻接计算路径，同位置焊接只用于分析图：

- 默认源连接图：22,901个选定管线顶点全部可达，最短表面路径长1.822841源单位，经过环的右侧。
- 仅在分析图屏蔽右弧局部后：21,797个剩余顶点仍全部可达，替代路径长2.057844源单位，经过左侧。
- 实际查看两张图：`artifacts/full-script-model-import/scope-surface-route.png`、`scope-surface-alternate.png`；相应NPZ保留图参数与顶点对应。
- 这证明同位置接缝合并后的图存在左右两条通路，不证明真实器械有分叉，更不授权删除其中一段原网格。下一模型步骤须区分源接触区域/真实管段并制作语义分段，不能继续用单条二维最近路径直接压直整个环。

整姿CLI新增 `--draft --require-valid`，真实原模型复跑返回 **exit 2 / REJECTED**；未给出该选项时仍允许输出明确标为实验的诊断产物。判定要求所有网格均通过，空报告、缺失结果或只有标签通过均拒绝。检查器测试现为 **7/7通过**，实际草稿仍失败，二者不混淆。沒有修改原FBX、运行入口或Assets，ST-01未签收；工具不是完整临床形态、法线/切线或Unity发布验收。

## 后续整姿实验：第一版已生成，未通过发布门槛

本次不再停留于文件清单：`tools/prepare_scope_pose.py` 直接读取原控制顶点，以源坐标中心线弯曲映射制作派生姿态，保留顶点顺序和原多边形索引；控制头/标签采用刚性变换。原FBX及材质/UV没有覆盖，输出仅为artifacts下的NPZ和几何核查PNG，尚不是带材质的Unity发布资源。

- 源坐标核对图：`artifacts/full-script-model-import/scope-pose-coordinates.png`。
- 第一版变形：`artifacts/full-script-model-import/scope-hanging-draft.npz`、同名PNG。已实际查看：外轮廓垂下，但连接线局部有异常分叉/拉伸。
- 门槛报告：`artifacts/full-script-model-import/scope-hanging-draft-gates.json`。本体141条边超过2倍拉伸、86条压缩到半长以下，最大653.82倍；无新增退化三角形，但仍明确失败。99百分位约1.174不能掩盖少量严重撕裂。
- 最大拉伸集中在源坐标约(0.19–0.20, 0.25, 0.02–0.05)，说明二维最近中心线/空间阈值分区在此处不足以保持邻接关系；下一步须检查交叠区的实际连通和分段归属，不能靠放宽发布门槛隐藏问题。
- 标签边长保持为1倍，刚性部分通过；不能据此签收本体或标签在最终整姿上的贴附。
- `tools/test_prepare_scope_pose.py` 5/5通过，覆盖刚性不变、拉伸拒绝、塌缩拒绝、源数组不变及中心线端点保持。测试证明检查器行为，不代表草稿通过。
- 草稿没有写入Assets、Resources或正式消费者，ST-01仍未完成。未构建APK、未运行设备。

本次是资源与拓扑核查，不是悬挂模型交付。未修改源 FBX、贴图、材质或运行入口；ST-01 的竖直镜体缺口仍保留。

## 已核实

- `ImportedModels/Source/Gastroscope/Gastroscope.fbx` 为 FBX 7400，SHA-256 `e288bd041556a8ecb9c4faf22113082528aeb0b6d07089cdb8e436309d8626b0`。
- 原模型已有底色、法线、金属/光滑度和标签绑定，不属于缺失全部材质的模型。现有 Unity 预览为盘绕姿态。
- 源文件有本体、标签两个 Mesh，无 Deformer、骨骼或已提供的整姿动画。本体 54,984 个控制顶点、97,152 个三角形；标签 960 个四边形，对应 Unity 的 1,920 个三角形。
- 本体按原索引分成 154 个连通分量，但仅为核查而按同位置焊接后连成一个表面；不能将 154 个分量当作可独立摆放的实体管件。位置焊接没有写回资源。
- Models.zip 的独立模型路径未发现另一个明确命名的直立版胃镜；进一步核查 `CleaningRoom_Repaired/CleaningRoom_Repaired.fbx` 的 131 个 Mesh，生成带原模型名、面引用顶点边界的几何核查图。可见主要是设备、门、灯、墙体和窗框，尚未识别出可直接取用的悬挂胃镜。
- 低面数建筑只画顶点会丢失轮廓，因此补画原多边形线框；密集网格使用确定性顶点抽样。核查图不是材质渲染、不是新增教学图，也不能证明全部资产中绝无可用部件。局部原始单位未转换为真实米制，不据此确定医疗器械尺寸。

## 后续实施约束

1. 若继续用此原胃镜整姿，需实际定位控制部、插入部、连接线和接头，在保留原 UV/材质/标签的前提下制作派生网格或绑定；不得整体旋转盘绕模型后标为“竖直悬挂”。无骨骼不等于无法后续绑定。
2. 需验证管身连续、无穿插、标签仍贴附、挂点与柜体匹配；先静态预览，再接入当前房间及卸载生命周期。
3. 未完成前不解除 ST-01 资源不可用状态、不计学员跳过、不把空柜或 V07 示范图当实体完成证据。

## 证据与验证

- 完整胃镜拓扑：`artifacts/full-script-model-import/Gastroscope-topology-audit.json`。
- 清洗室几何索引：`artifacts/cleaning-source-inventory/mesh-index.json`；同目录 `source-meshes-1.png` 至 `source-meshes-3.png`，三张均已查看。
- 只读工具：`tools/audit_scope_fbx.py`、`tools/render_fbx_inventory.py`。
- `tools/test_audit_scope_fbx.py` 本次 **7/7 通过**：压缩数组、边界拒绝、同位置接缝与原数组不变、真实模型哈希和与既有 Unity 三角形数对照。
- 二进制读取仅支持 7000≤version<7500；不执行 FBX 内容，不作为完整 FBX 导入器。编码依据：[Blender 的 FBX binary format 说明](https://code.blender.org/2013/08/fbx-binary-file-format-specification/)。
- 本次没有 Unity 运行代码变更，没有构建 APK，也没有 Quest 实机验证。此前 63/63 是柜侧登记版本的证据，不是本次悬挂镜体完成的证据。
