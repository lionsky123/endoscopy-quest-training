# C08 当前状态 · 完整复刻植物园流程，替换教学主题

2026-09-17 最新交互：第 1 关取消主题、图片翻页和媒体模式选择，改为房间观察后按固定顺序动态加载六张图谱（门、设备、流程各自的现场特写与正误对照）。空间中一次显示一张图，保留简短说明和一个近触继续按钮；最后一张直接进入 3 道巩固题。原精灵带路和主动换站保留。以下 9 月 16 日面板记录为历史方案，当前以第 1 关方案的“当前内容与顺序”章节为准。

最新调整：第 1 关已改为房间全景观察、按需特写和 3 道巩固题，见 [第 1 关方案](FIRST_LESSON_ROOM_OBSERVATION.md)。三组照片风格的正误对照图已接入学习页和全景，覆盖门、设备分设、单向流程。面板已按参考工程改为深色圆角图文布局，全景已修正完整纬度映射和近球面双眼视差。最新针对性 EditMode 测试 9/9 通过，报告 `artifacts/first-lesson/first-lesson-ui-projection-tests.xml`；固定视角渲染检查通过，实际房间加面板预览见 `artifacts/first-lesson/first-lesson-immersive.png`。未构建或部署 APK。后五关保留原样；下文未更新处为此前 C08 记录。

2026-09-16：主工程 app 已切换为参考工程的完整副本，沿用原六点路线、导航、小精灵、对话和六站流程。最新调整取消图鉴、工具/地图召唤与入册步骤；带路自动显示路线与目标。三组正确示范/错乱案例共 10 道修复题；使用已有清洗消毒图谱、参考图片和用户全景。学习面板使用真实手部近触，全程静音。原房间不加载，文件在旧工程归档中保留。

最新去图鉴调整验证：Android 目标外部隔离工程 EditMode 测试 5/5 通过，报告 `artifacts/replica-c08-no-atlas-tests.xml`（2026-09-16 18:59）。六站完成/跳过分支不产生收藏弹窗，主动离站与路线可见性通过。未构建或部署 APK。

初次复刻的历史验证（早于本次去图鉴调整）：针对性 EditMode 测试 7/7 通过，报告 artifacts/replica-c08-tests.xml；界面预览 artifacts/replica-c08-preview；核心文件与源工程一致性报告 artifacts/replica-c08-core-comparison.json。没有编译或部署 APK，不能据此声称头显已更新或整流程真机验收通过。本机 PlayMode 的 MRUK 原生初始化失败，采用隔离的编辑器测试和渲染进行验证。

当前源码 0.9.0 / C08，包名 com.endoscopy.inspection。旧工程备份 archive/app-before-template-C08-20260916。后续只编辑 app，保持参考工程原件只读。详见 [C08 说明](FULL_TEMPLATE_C08.md)。

以下为历史状态，旧 CircleLesson 和 C07 局部复用已被替代。

---

# C07 当前状态 · 参考地图与 Meta 近触

2026-09-16：接入 BotanicalGardenQR-Base 原六点地图、六段曲线与导航控制器；小精灵带路、落后等待、位置追踪暂停、玩家实际入圈后开始学习。地图可近触打开/收起，主线仍为三组正确示范与修复问答，模型保持隐藏，全程静音。

面板使用 Meta PokeInteractor/PokeInteractable。实际尺寸按钮快速穿越原先漏触的回归已通过，持续按住不重复提交，取消不提交；鼠标与 Submit 入口仍被屏蔽。短引导随精灵移动，对话距离改为 0.52 米。

验证：artifacts/reference-c07-tests.xml，8/8 通过，结束时间 2026-09-16 09:32:06 UTC；覆盖真实 SDK 表面事件、问答成绩、跟随步行、模型隐藏、地图 GLB 加载、MR 特性/地图着色器和原生构建准备中的 Composition Layers 校验。没有运行 Android 构建。预览：artifacts/circle-preview/circle-intro.png、reference-atlas.png。

本轮未构建、安装或启动 APK。只读设备查询得到中文包 0.8.1 / code 13，未证明设备已运行 C07；编辑器模拟不能代替真机手部追踪。原地图 scale=2，点位跨度 10.6×10.1 米，需要匹配的实际场地。

当前方案：[C07 复用说明](BOTANICAL_REUSE_C07.md)。以下为历史状态。
# C06 当前状态 · 隐藏模型，分散六关

2026-09-16：房间模型从创建起隐藏，底座取消；点位改为两排路线，相邻间隔 2.2 米。预览显示六点，开始后仅显示当前关卡，精灵实际带路。玩家走在精灵前面时精灵继续前往目标，只有玩家落后过远时等待。全景退出仍保持房间隐藏。

针对性编辑器检查 7/7 通过（artifacts/level-c06-tests.xml，2026-09-16 09:00:08 UTC），包括模型隐藏、点位间距、当前关显示、步行推进、玩家走得较快、全景退出、问答计分和 MR 构建配置。预览见 artifacts/circle-preview/level-route.png。未构建、安装或启动 APK。

当前设计见 [C06 六关路线](LEVEL_ROUTE_C06.md)。以下为历史状态。
# C05 当前状态 · 2026-09-16

当前源码入口为 CircleLesson：MR 桌面展台、六圈实际步行引导、三组示范/修复题，合计 10 题。已导入用户提供的全景、两张原创教学图谱及既有局部照片；视频仅播放器与候选来源，尚未装入教学片段。

验证：artifacts/circle-tests.xml，8 项通过、0 失败，结束时间 2026-09-16 08:43:52 UTC。包含 5 项新课程检查和 3 项构建身份回归；不是完整测试套件或真机验收。当前桌面无头显透视画面，预览黑色背景不能代表实际 MR 效果。

预览目录：artifacts/circle-preview。用户全景原件已复制入工程，不依赖微信临时目录。

没有构建、安装或启动 APK。源码 C05 不代表设备中的中文包已更新。真机透视、地面高度、现实路线及实际手部输入仍待用户自行构建后确认。

[C05 设计与素材来源](CIRCLE_LEARNING_DESIGN.md)

以下为历史状态。
> 当前有效设计为 **C05：MR 桌面展台 + 小精灵实际带路 + 六圈问答**。用户已明确覆盖此前的禁止答题/光圈及关门、搬物主线。参见 [C05 设计与实现](CIRCLE_LEARNING_DESIGN.md)。以下 M04 及更早内容为历史记录，不能当作当前入口。未构建或安装新版 APK。
# 当前项目状态

更新：2026-09-16。源码为 0.8.1 / M04，未构建 APK。用户授权的默认包 `com.DefaultCompany.app` 已卸载并立即核验；中文 `com.endoscopy.inspection` 保留，仍是此前设备上的旧面板版。

## 当前源码

- 镜头一先看合规布局，再纠正一次已有的未关门状态，握住真实把手合门松手即可。
- 删除示教盒搬运和人造待处理台。设备与流程用原场景实物关系、可选近触说明及顺向演示呈现；无面板答题、强制点遍或重复复查。
- 观察进度与实际模型操作分别记录，不把看过视为独立掌握。六站框架保留，其余五站旧交互尚未完成重设计。
- 固定构建身份，显式 Android 构建时生成递增 code 与唯一标识，成功后写 APK 哈希回执。此构建路径尚未实跑，用户自行构建。
- 安装核验工具默认读取当前设备哈希；无设备报错，不把旧副本当实时结果，不默认把 1.apk 当新版。
- 静态检查通过；定向结果见 `../artifacts/layout-m04-tests.xml`。未做本版 Quest 抓握验收，未编译或安装新版。

设计见 [M04 教学设计](SHOT_01_TEACHING_DESIGN.md)，设备操作见 [排查记录](DEVICE_INTERACTION_DIAGNOSIS.md)。

## 历史状态（以下不作为当前版本判断依据）
# 当前项目状态

> 最新状态（2026-09-16）：用户反馈模型仍不能互动、关卡设计奇怪。已查明设备中文包是旧面板版，本地模型版未安装；当前关卡退回设计复核，不能再称为已完成/已认可。源码新增 0.8.1 / code 10 与 M03 原型标识，设备及现存 APK 未改。详见 [设备排查记录](DEVICE_INTERACTION_DIAGNOSIS.md)。

更新：2026-09-16。版本号仍为 0.8.0 / code 9；镜头一源码已重做，尚未生成新包。当前依据：[镜头一实现记录](SHOT_01_IMPLEMENTATION.md)。

## 本轮镜头一改动

- 第一关改为逐步带看与门把手拖动、专用设备归位、末端交接三个模型练习，移除第一关球、分类卡投放和五点顺序点击。
- 原门洞保留，入口一个按钮带到门前；新增目标文字、把手定位引线和门旁 Oppy。门扇独立铰接，实际握住把手后按手部位移转动，松手关闭才完成。
- 引入 Oppy 模型、动画、肖像及深色金边短对话。静音、真实食指触碰和主动换位仍保留。
- 补充可观察的隔断开口、两类设备配置与流程变体；操作期间没有答案/提交按钮，错误设备归位和回流在模型上重新拿起纠正；记录为模型练习完成。
- 通过全部 Runtime 及已更新 Unity 回归脚本的 C# 静态编译、160 项核查/门把手逻辑断言、导入资源 GUID 闭包检查。随后执行镜头一定向 Unity 测试，1 项通过（`../artifacts/layout-model-tests.xml`）；编辑器截图位于 `../artifacts/layout-preview/`。未执行 Quest 验证。
- 按截图修正入口先看全景、对话框下沿裁切、机器观察距离和铭牌大小。定向测试加载正式场景并明确使用教学相机，修正临时测试场景相机导致的无效截图；该问题不属于正式场景重复相机。
- 后五站仍是旧版八项动作。以下旧版测试结果仅记录历史，不能覆盖本轮改动。

## 旧版 0.8.0 历史实现与编辑器检查

- 默认进入清洗消毒室六关/11项动作教学：现场亮点触碰、资料捏取拿近、分类放置、双面翻转、记录缺项对照。
- 面板仅接受近距离 XR Hands 食指尖触碰，屏蔽鼠标/射线点击及 Submit 事件；默认入口不创建 EventSystem 或图形射线组件。
- 资料必须先查看再放到匹配目标框，追踪丢失取消拿取，不计为完成。
- 双面资料根据阅读方向切换可见面，修复背面镜像文字透到正面的问题。
- 全程静音，包括教程。项目关闭 Unity Audio，运行时停止并禁用音源。
- 保留原房间模型，真实走动与转身；跨关由用户确认后淡出换站。无摇杆/WASD移动。
- 统一渲染管线、Linear/Vulkan、Android 包名/产品名/版本/ARM64/IL2CPP 设置，支持用户从 Unity 原生入口自行构建。

## 实际验证结果

- Unity 编辑器测试：30 项通过，0 失败。结果：`../artifacts/hands-on-tests.xml`，日志：`../artifacts/hands-on-tests.log`。
- 动作流程使用模拟手部姿态覆盖 11 项推进、未查看不能完成、错误记录不通过、翻面可见性等；画面：`../artifacts/hands-on-preview/`。
- 目视核对了资料正面、翻转背面和维护记录页，阅读方向正常，无镜像背面重叠。
- 以上均为编辑器验证，不替代 Quest 真实手部追踪或佩戴验收。

## 未完成与限制

- 毛刷、滤膜、灭菌附件包装和真实过程视频未齐。当前卡片、路径及记录示例明确标为教学资料，不称为完整临床操作仿真。
- 图片热点只覆盖照片能提供的证据，不能证明五类工具、供水连接或测漏功能全部合格。
- 尚未佩戴确认本版捏取距离、遮挡恢复、触碰手感、双眼阅读、空间适配与帧率。
- 其他区域暂不实施，完整甲方五场景验收未完成。

## 安装包状态

本轮未构建 APK、未安装、未启动 Quest 应用。`../builds/EndoscopyInspection-Quest3.apk` 仍是 0.7.0 / code 8，文件修改时间为 2026-09-15 21:08:59。其 release-info 保留原值。

新改动位于 `D:\quest3\EndoscopyBuild\app`，由用户自行编译后测试。英文路径是中文项目目录的联接，二者是同一份文件。











2026-09-16 C05 修复补记：Composition Layers Support 已启用并保存；原生 Build And Run 前自动补齐 MR 依赖。针对性检查 5/5 通过，记录见 artifacts/composition-fixed.xml；未构建 APK。

