# 原地游玩重构与资源核查

## 测漏登记逐行定位与返回

`FullScriptOfficeRecords` 的3条纸表登记增加真实近触区域，按稳定UseId定位完整使用记录，不依赖当前筛选/行号。显式关联跳转清除旧日期/镜号筛选，保留面板世界姿态；返回资料恢复原纸表并以轻选中状态及使用号提示保留对照上下文。第四未使用行无操作区域。办公室和洗消只读页共用该行为，不记录字段查阅、不填写判断、不开始OF-01，不自动完成OF-02/RE-05。

`leak-row-links.xml` **29/29通过，28.746秒**：新增办公室/洗消×带教/独立4例真实SDK近触，每例连续对照3条，穿插筛选以验证隐藏记录也能正确定位；包含原有同源、错页、数据/会话回归。静态20单元与路线校验通过。两张更新的纸表截图已核对，近触提示/来源声明与表格、按钮不重叠；选中行往返由运行测试验证，本轮截图为未选中初始页。V08及数据未修改、无新资产，无APK；不把便利查阅算作完整测漏核查。

## 测漏登记从文字页改为既有纸表载体

读取图册并查看V08空白纸面后，复用该图的实际五列表线，不生第二张同类图、不自绘表线。`BuildLeakPaper` 为办公室与洗消室共同消费者，五列分别呈现登记号、使用/镜号、时间、人员、结果，3条15格，第四行保持未使用并显式说明。普通资料页保留原目录，洗消只读页保留使用记录对照与返回。模拟来源与内容边界持续可见。

`office-training-records.json` 升级SIM-20260921-2：原正文中的三个记录值迁移为 `leakEntries`，正文只保留范围和来源说明。镜号直接取关联使用记录。`ClinicalTrainingRecords` 提供不可变条目/副本数组，配置阶段校验唯一登记号、唯一使用号及对应关系，拒绝错误配置时不覆盖已载入版本。旧课程与模型未修改，未新增医学结论或真实记录。

最终 `artifacts/vr-interaction/leak-paper-reuse.xml` **25/25通过，20.210秒**；包含原位近触两模式、办公室错页回归、纸面复用/15格/关联镜号/第四行说明、数据关联拒绝及既有记录会话回归。静态20单元与路线校验通过。实际查看 `stationary-final/office-leak-register-paper.png` 和 `washing-linked-leak-ledger.png`：长镜号在格内、行列对齐、来源/范围/按钮均可见；本轮未改变固定观察者来迁就页面。

V08 SHA256核对未变。该纸表是模拟样式，不冒称医院原图还原；完整测漏设备、正式资料、附件包装与独立判断仍未完成。Android目标编辑器验证，无APK、无设备安装或Quest实机验收。

## RE-05就地对照办公室同源资料

第二细项新增 `OpenLinkedLeakRecords`，延迟创建共享 `FullScriptOfficeRecords` 的sourceOnly视图；同一 `ClinicalTrainingRecords` 版本/正文与V06原稿加工屏，无另一房间或实体终端。使用页显示SIM-R与SIM-L对应关系，可筛选/翻行/返回登记；只读视图没有字段判断、学完或跳过按钮，亦不修改OfficeFieldsViewed/OfficeRowsViewed/任务状态。包装细项不显示本入口。房间主动导航关闭媒体，离房Dispose释放该消费者。

针对性报告 `artifacts/vr-interaction/washing-linked-records.xml` **24/24通过，19.287秒**，含两模式真实SDK近触、正文同源、使用号关联、无终端、无评审/进度串扰、固定页位、返回和离房关闭；办公室路由及记录/旅程回归一起通过。静态检查通过。新增实际截图 `artifacts/stationary-final/washing-linked-leak-ledger.png`、`washing-linked-use-records.png` 已查看，正文/原屏表格/对应登记号/完整操作按钮可见；未为截图移动观察者。编辑器Android目标结果不是Quest实机验收。

边界：本轮没有新生图/自绘表格，登记页直接复用已有模拟文字，尚不是原医院纸表的视觉还原。正式登记、独立缺项判断、测漏设备与附件包装仍未齐备，OF-02/RE-05保持原不可用等级，更新说明避免误说“已有模拟资料完全缺失”。无APK构建。

## 办公室对应资料入口修复

发现正式入口 `Mathf.Clamp(_scriptIndex-2,...)` 将OF-02/03/04/05分别打开清洗消毒/测漏/生物学监测/消毒剂监测，错了一类。改由 `ClinicalTrainingRecords.DocumentIndexForTask` 按已发布资料ID查找，避免排序耦合；缺失对应正文时按钮明确待补且不回退其他材料。OF-00保留通用资料入口，OF-01单独电子记录入口。

`FullScriptOfficeRecords.BeginStationary` 不再无条件调用开始OF-01的方法；资料查阅与电子字段任务分离，用户主动返回电子核查页才尝试开始OF-01。没有自动完成、追加字段引用、恢复旧课程或解除不可用内容状态。

验证：`office-doc-route-red.xml` 4项全部失败，实际标题均为前一类；修后 `office-doc-route-fixed.xml` **22/22通过，10.635秒**，包含四个真实SDK近触入口、原位页面姿态、资料重排/缺件、记录与旅程会话回归。20单元静态及路线校验通过。版式与素材未改，因此本轮未新增截图；这些测试不代表正式纸质档案、完整资料情境或Quest设备验收。没有APK构建。

## 原胃镜源拓扑核查（未变更运行姿态）

已核实本体97,152三角形、标签1,920三角形与既有Unity导入结果一致；源文件无Deformer，原材质/贴图完整。同位置接缝核查后本体连通，不能直接拆154块或旋转整件冒充悬挂。另逐项输出清洗室源包131个Mesh的几何图与索引，未识别出可直接取用的悬挂模型。只读工具7/7测试通过；未新增生产图、未修改运行场景，ST-01保持未完成。完整边界及后续整姿要求见[GASTROSCOPE_SOURCE_AUDIT_20260921.md](GASTROSCOPE_SOURCE_AUDIT_20260921.md)。

## 2026-09-21续作：柜侧登记实体入口

共用按钮显示修复证据：完整旅程新增尺寸使圆角缓存达到既有限额，`CreateRoundedSprite`备用图的pixelsPerUnit=1与Canvas默认100不一致，18px边角被放大为1800 UI单位。`storage-register-shape-red.xml`4/6、1.892秒，强制缓存耗尽时填充/轮廓两用例均红；只改备用图单位为100，不增加缓存数量或像素预算。最终63/63及原旅程截图按钮角内像素检查转绿，人工确认恢复完整圆角矩形。失真截图留在`artifacts/storage-register-before-shape-fix/`；没有改动V08或重新生图。

上一轮为有效进展：ST-02程序模拟记录、V08消费者、55/55回归及实际截图已落地；本轮继续解决柜侧入口缺口，不重复生图。

`FullScriptStorageRegisterMount`将同一V08纸面固定挂在TrainingStorageCabinet左外壁，局部x=-.622m/y=1.2m，宽.36m、高.24m，朝左侧通道；比外壁x=-.6175m外移4.5mm避免共面闪烁。它是纸面登记载体，不是可翻页多页装订本，本情境本来只有1页。`BuildStoragePaper`为挂载/放大视图共享渲染消费者，五列/四周/来源声明完全同源，带教与独立数据仍分离。

ST-02增加“查看柜侧位置”，主动遮黑后环境对齐到纸面前60cm，不移动真实头手/原点，不随转头回中。近触实体纸面打开已接登记页，关闭返回原概览也经遮黑；原有52cm处直接打开为原位可达阅读入口保留。只有柜侧观察时纸面接收输入，其他面板/转场时关闭输入；挂载随房间生命周期释放，先销毁载体再释放专用字体材质。

诊断证据：首轮旧55项通过，新2项误用“必须成功”的触碰助手验证禁用状态；修正负向测试后`storage-register-mounted-gates.xml`57/57，但实际截图完全被挡，不能验收。遵循diagnosing-bugs建立`tools/check_storage_register_capture.ps1`，两次重复红灯：蓝色控制/纸面样点0/0。临时视线探针定位右侧18.08cm处原建筑`CorridorEntrance6`遮挡，眼/纸面方向计算正常。只切到左侧后，`storage-register-left-visibility.xml`2/2，截图纸面2647样点可见、蓝色按钮0，门体遮挡消失但52cm操作条仍落在50.45cm柜侧板后。随后仅将观察距离改60cm，保留门体/柜体/原相机，不隐藏几何；原50cm失败截图保存于`artifacts/storage-register-occluded/`与`storage-register-left-first/`。`storage-register-visible.xml`57/57、84.710秒，视线/纸面/操作条可见性通过；但人工截图发现按钮椭圆，未据此签收页面。临时探针已移除，永久增加实际纸面/操作条/放大页三条视线回归和截图门槛。

最终静态20单元通过，`storage-register-final.xml`63/63、83.875秒；两模式同源/真实手触/固定世界/卸载/提交后实体入口只读，以及三条不透明几何视线检查通过。两张最终运行截图已核对纸面、完整操作条与按钮外观，截图门槛也通过（柜侧蓝色/纸面样点334/3469，放大627/6142及两处按钮角内填充）。进程正常退出，验证专用缓存清理完成。未构建APK。仍不签收ST-01缺失悬挂镜体、写实污渍/干湿情境或全项目最终质量。

## 2026-09-21续作：ST-02连续清洁登记

本节为上一轮记录；其“柜侧实体入口仍待接”已被上方本轮63/63及实际可见截图覆盖。仍保留模拟资料身份及其他储存任务缺口。

核对旧稿O-S06文字与已有图册后，新增V08写实空白登记纸面；未找到对应原始医院表图，明确标“模拟训练记录/非医院原表”。程序单源`StorageCleaningRecords`填入5列，数据范围2026-08-24—09-20，共4周/1页/无另附登记。带教每周完整；独立第3周日期/登记内容/操作人空白，对象/范围保留。该缺项只支持“缺少登记”，不支持实际未清洁结论；每周频次只属剧本情境。

`FullScriptStorageRecords`作为正式ST-02消费者，固定原位近触逐周选中后保存判断和该周证据，4个独立criterion防止直接总判断绕过。带教需看完4周再显式确认，可明确跳过；离开只保留进度不代判。通过现有Session统一在办公室提交、锁定；`ClinicalRecordReview`追加4周复盘，不提前揭晓判断结果。打开/读完不自动完成，原有C09不接回。

V08和完整prompt/哈希/实际接入状态已同步图册、spec、plan。静态20单元通过；首次`storage-cleaning-records.xml`54/55，旧复盘测试仅假定6字段；第二次`storage-cleaning-records-review.xml`54/55，扩大到10项后捕获新增周复盘文字溢出。最终精简重复范围语句，保留依据/缺项边界、不缩字号；`storage-cleaning-records-verified.xml`55/55，74.709秒，覆盖10项逐页无溢出、两模式近触/四周记录/修改/只读、新会话空记录等。验证进程已正常退出并清理验证专用缓存。

已核对`artifacts/stationary-final/storage-cleaning-register.png`及`storage-cleaning-register-selected.png`：五列表线/文字吻合、选周高亮可见、四个按钮完整留在固定坐姿视野内。1.2m固定观察者/Android目标/Vulkan编辑器，不是Quest验收。当前从原位任务入口打开表，不是柜侧挂本实体交互，后者仍待接；ST-01内镜悬挂和全项目视觉缺口不被本项Ready替代。没有APK构建或设备部署。

## 2026-09-21续作：储存/候诊房间局部照明

核对实际消费者：两房门/灯材质来自已隔离原表面，地面已有水磨石，储存柜有独立涂层/钢/玻璃材质；不是所有表面都缺贴图。此前VirtualRoomEnvironment仅给统一Flat环境光，两个派生房间没有实际局部Light组件，画面层次不足。

现仅为共享原Office建筑布局的Storage/Waiting增加4个顶棚Spot近似光源（局部y=2.70m，x=±1.4m，z=±1.25m），范围4.5m，不投射阴影，不新增阴影贴图；配中性三色环境光。灯位是训练布局照明近似，不是实地灯位/照度测量。没有修改原网格、材质、贴图或其余房间；大厅高斯不受此分支控制。

所有局部光源挂当前房间根，原位观察切换与建筑一起移动，仅遮黑期间执行；转头不会跟随。卸载随房间移除，释放时恢复之前的环境光模式和三色值。既有URP每对象最多4附加光源，未提高管线限额；Quest GPU成本尚未测量。

前版截图保存到`artifacts/room-lighting-before/`。第一版`derived-room-lighting.xml`50/50通过，73.139秒，含两房材质引用一致性、光源数量/类型/无阴影/固定位置/卸载与环境光恢复。`artifacts/room-lighting-first/`保留第一版实际截图：百叶、门框和椅子曲面细节可见，但天花偏暗、墙面光斑过强，不能算视觉验收。

第二版仅调整该照明分支：三色环境补光提高到(.68,.68,.67)/(.56,.56,.55)/(.49,.49,.48)，Spot强度2.3降至1.1，外/内角115/85改140/120，色彩中性化。数量、范围和无阴影限制不变，原表面未改。最终`derived-room-lighting-balanced.xml`50/50通过，72.053秒；静态20单元通过。

已人工核对最终`stationary-final/storage-cabinet-overview.png`、`storage-cabinet-open.png`、`waiting-observation.png`、`waiting-corridor-observation.png`及前版：天花/墙面明暗比第一版均衡，百叶、门框、椅子曲面和柜内表面有局部照明层次。仍有明显光斑，缺接触阴影/真实反射及部分表面细节，办公室椅类型与空柜缺项未解决；仅保留为当前视觉改进，不签收全项目写实质量。未生图、未构建/安装/启动APK，Quest性能仍待实机验证。

## 2026-09-21续作：胃镜与消毒剂独立实物观察

ST-03和RE-04原先自动在说明面板前加载最长边23cm模型。现由“收起说明，检查实物”主动加载原模型，改38cm展示副本与低位操作条；胃镜调整初始展示朝向以看见盘绕表面，不改变原网格或冒充竖直悬挂。消毒剂继续使用用户带标签原瓶，不恢复通用瓶。

保留源网格、材质和贴图，未新生成图片。真实手部可拿起转看；移远不再按与头部的距离自动瞬移，改主动近触“取回观察物”回到固定初始展示姿态。返回说明关闭模型，重新打开才加载观察实例。提交后回看禁用抓取与取回；这些动作均不完成ST-03或RE-04，不凭标签外观作产品合规判断。

首轮`source-object-inspection.xml`43/43通过（64.891秒），覆盖两个模型的实际近触、源网格/材质引用一致性、38cm显示边界、主动取回、返回清理及提交后只读。截图随后发现低位操作条底部裁切，且瓶体默认朝向背面，不能据首轮测试宣称视觉通过。

新增与实际中性坐姿视点一致的投影门槛后，`object-toolbar-framing-red.xml`2/2失败，操作条底角viewport.y约-0.032549。排名假设为布局过低、相机参数不一致、模型遮挡；布局解析投影与失败数值吻合（相同78°视野/.52m距离），排除仅遮挡解释。将条位置从说明位下24cm抬到17cm，底角回到约0.05；未改变玩家/头手。瓶体观察副本初始绕竖轴转180°，原贴图/网格不修改。`source-object-framing-verified.xml`43/43通过，61.137秒；截图确认完整按钮与瓶身标签可见。

第二次截图发现瓶底仍贴近说明条上缘。新增净距门槛初版以世界AABB投影过度扩大旋转后的空白区域，改为逐Renderer.localBounds经自身Transform投影，而非放宽阈值。`object-local-clearance-red.xml`2/2失败：胃镜/瓶体保守下边界分别0.192228/0.150129，需大于说明条顶部加1%净距0.199495。将模型中心从说明位上11cm抬到17cm，操作条不再移动。最终`source-object-clearance-verified.xml`43/43通过，61.001秒，静态20单元通过。

最终Vulkan截图`artifacts/stationary-final/gastroscope-object-inspection.png`和`disinfectant-object-inspection.png`已人工核对：原胃镜几何/纹理、盘绕状态和瓶体正面标签可见，模型与说明条之间有净距，取回/返回按钮完整。展示只验证观察副本，不证明临床情境、悬挂状态或产品适用性；场景材质/光照仍待完善。无临时调试日志/原型残留，未构建、安装、启动APK；没有Quest真实手部或设备性能验收。

## 2026-09-21续作：新稿毛刷细项消费已有素材

核对旧稿场景5镜头二和已有资源后，发现RE-02第三细项的毛刷文字仍配同一设备总览，而`ClinicalCourse/brush-inspection.png`只留在旧课数据中。现改为按细项选择：前两项仍使用0920设备原图，第三项使用已有毛刷生成教学图。图中上下两把、芯线及刷毛已人工查看；不重新生图或手绘，不把图像当现场型号凭证。

带教第三页可真实近触依次切全图/刷毛/芯线取样范围，按取样宽高比显示并放大；图像原件未改。生成来源在普通正文与展开页均显示，展开图限制在按钮上方。切细项/任务会复位局部范围和放大状态，避免设备图继承毛刷特写或正文被隐藏。独立核查不显示带教图或局部按钮；只记录RE-02:2查阅，不自动完成父任务，不恢复旧course.json及旧课程模块。

补登记图册B01来源、哈希和消费者。柜侧每周登记目前仅查到旧稿文字，尚未定位可复用原表，本轮不虚构医疗记录列表。静态20单元通过；Android目标编辑器`latest-brush-consumption.xml`41/41通过，56.932秒。新增两模式用例使用实际SDK PokeInteractor验证图片选择、三种取样范围及比例、固定面板、展开来源提示与按钮净距、切回复位、查阅不完成任务、旧课程模块未恢复、离房无旧图片组件。

Vulkan截图`washing-brush-overview.png`、`washing-brush-bristles.png`、`washing-brush-wire.png`已人工核对：上/下毛刷全貌及局部可见，生成来源明确，图像未盖住底部按钮。背景保留已有清洗室模型，表面光照仍非最终视觉验收。测试和预览不是Quest真实手部/内存验证，未构建、安装或启动APK。

## 2026-09-21续作：储存三维建筑与双门柜

正式原地R02_STORAGE改为`FullScriptRooms/Storage/StorageRoom`，不再选择FurnishedResource的程序方块房/单块柜。建筑取已发布的原OfficeRoom派生墙、地、顶、灯和门；不复制候诊椅/隔断，不运行加载整个OfficeRoom/WaitingRoom。显式`StorageRoomPublish`按材质/门合并柜体网格并发布，不调用构建或自动编辑器回调。

柜体是按V07补建的通用结构训练模型，约1.23×.64×2.2m（宽深高），尺寸不是实际设备或厂家规格。具有内壁、底板、顶板、支脚、通风开孔/百叶、双玻璃门、密封边、把手与固定铰链；柜内安装横梁保留空位，没有把盘绕胃镜改名当竖直悬挂。门外壳和内壁不是一个实心占位方块，材质单独区分粉末涂层/钢/玻璃/密封橡胶。表面和光照仍须运行截图检查。

ST-01近触“检查柜体”收起说明，再用真实手部抓取两侧把手；左右门分别限制0至105°、-105至0°。实际双门均展开≥70°并保持0.4秒才记`ST-01:cabinet-opened`，只记动作，不完成任务、不生成医学合规判断。返回说明禁用柜门抓取，离房沿既有释放链清理。使用稳定的同父级铰链pivot，避免pivot随门转动造成角度反馈。没有缩小玩家/头手，也不要求走到柜前。

ST-01保持RuntimeUnavailable，原因更新为柜体双门和内壁已接、竖直镜体及完整核查待补。V07仅带教图像继续使用，独立模式仍不泄露示范。新增回归覆盖实际资源依赖、无整套FBX/候诊预制依赖、真实近触观察入口、SDK旋转35/80/130°输入的角度上限、铰链位置稳定、双门动作不冒签和返回后抓取禁用。

首次`storage-structural-cabinet.xml`38通过/1失败，依赖检查发现原办公室FBX（GUID `584343b84301f7148ad8b9c2e9da15ab`）仍由门/灯的`m_Materials`直接引用。不是网格未拆，也不是整个Waiting prefab连接。`RoomMaterialPublish`保留源材质设置、复制为独立.mat，若有FBX内嵌贴图则拒绝静默丢弃；Storage和Waiting两个出版器均加递归FBX拒绝门槛。复出版后Storage递归依赖42项、Waiting43项，两者FBX均为0；分别保存`dependencies.txt`。这证明资源引用隔离，不冒称设备内存实测。

按已发布后墙折角包围盒，将柜体与近观察位一同移至x=-.9，避免柜体右半嵌入后墙；测试逐面比较柜体与PaintedWall包围盒无相交。原候诊椅排位置微调至不挤入前墙，源椅几何未改。柜体合计1828三角形，11组按门/材质合并网格；源文件和旧开发代码均保留。

`storage-cabinet-isolated.xml` **39/39通过**，47.022秒；静态20单元通过。`storage-cabinet-closed.png`/`storage-cabinet-open.png`已人工核对，可见把手、双门、内壁和空安装横梁。此近点便于伸手但看不全柜体，随后补整体/近处两个原位观察距离：整体位z=0、操作位读取已出版StorageObservation（z=1），近触触发遮黑，头手不动；整体位禁用把手抓取，返回说明恢复整体位。

最终距离切换回归`storage-cabinet-two-distance.xml` **39/39通过**，49.828秒，含原位位置保持、两个模型局部观察坐标、整体时抓取禁用、近处可操作与返回后恢复整体位。源SDK旋转用合成抓点驱动，近触入口用实际SDK PokeInteractor驱动；这不是Quest真实手部传感验收。静态检查再次通过，未构建/安装/启动APK。

最终Vulkan截图已核对：`storage-cabinet-overview.png`显示柜体上部全貌、玻璃门、把手和通风带，底部部分被低位操作条遮挡；`storage-cabinet-open.png`显示展开双门、内壁与空安装横梁。整体/近处切换解决近点裁切顶部与侧边的问题，但当前材质和光照仍偏平，柜内无悬挂镜体；不能据截图宣称完整写实环境或储存核查已验收。

## 2026-09-21续作：储存带教图与候诊斜向取景

核对图册与旧稿场景2后，内置imagegen生成V07柜体教学情境并保存到工程：`ClinicalCourse/FullScriptVisuals/storage-cabinet-teaching-v1.png`。ST-01带教页实际加载并支持放大，明确非实拍、不证明通风性能/管腔干燥；独立核查不显示它。不修改ST-01不可用状态，不以看图代替开柜、柜内检查或判断。生成图只是补充带教可视内容，储存库灰盒及三维柜体仍须继续完成。

放大页单独使用380px图高与下移中心，来源提示位于图上方，隐藏重复缺项段落避免重叠，但不遮挡底部按钮；普通页保留缺项原因。候诊两侧默认观察方向改为斜向（候诊侧朝左前、诊疗通道侧朝右前），不改变已有位置/隔断/头手比例，转场仍只在遮黑时执行。新增两模式图片消费/隔离和未冒签测试，候诊测试增加侧向朝向断言。

Android EditMode `artifacts/vr-interaction/storage-teaching-waiting-framing.xml` **38/38通过，0失败**，47.852秒；包括新增带教/独立两个实际运行用例、固定原位近触、旧入口隔离、完整路线/释放与静音回归。静态20单元检查通过。V07在Unity中按GUID成功导入，运行断言检查RawImage纹理资产路径；不是仅检查文件存在。实际画面另行核对，不以测试通过冒签三维储存或独立判断。

Vulkan运行截图已完成并人工核对：`artifacts/stationary-final/storage-teaching-expanded.png`显示V07与生成来源提示，图片未遮挡底部近触操作；`waiting-observation.png`斜向视点能同时看到一把完整座椅、其他座椅局部、隔断和地面，`waiting-corridor-observation.png`显示另一侧空通道及入口。仍不能据此认定完整人流关系已验收：候诊椅类型、真实材质/光照和更完整观察仍待完善。储存灰盒清楚可见，不宣称已消除；无APK构建或设备验收。

## 2026-09-21续作：原位双侧观察

候诊观察现在使用已出版的`WaitingObservation`和`ClinicalCorridorObservation`两个原模型空间锚点，不添加穿过隔断的步行路线。真实近触“切到诊疗通道侧/切到候诊侧”请求现有遮黑状态机，黑屏内对齐对应位置与面朝隔断的方向；XR头手与比例不变，亮屏后保持房间固定。按钮暂停到转场结束，返回说明不冒记查阅/合规。

观察条采用标准深色按钮和与正文相同的文字材质，修复此前截图返回按钮过浅的问题。新增回归检查两侧模型坐标、隔断位于视野前方、头部位置/旋转与XR根位置不变、环境亮屏后稳定；预览器增加`waiting-corridor-observation.png`。静态20单元检查通过；Android EditMode `waiting-two-sided-verified.xml` **36/36通过，0失败**，42.523秒。

初次报告`waiting-two-sided-observation.xml`为35通过/1失败：断言跨两次近触比较头部朝向，但`ClinicalHandFixture.Touch`会主动LookAt不同按钮。这不是已证实的运行时运镜。按诊断回路定位后改为近触完成时采样，每帧验证转场40帧不改变头部，并检查遮黑前不移动和期间拒绝重复请求；真实SDK近触保留，没有放宽姿态误差或修改生产代码掩盖红灯。复跑通过；截图另行检查，不以测试代替视觉验收。

Vulkan重拍完成并人工查看`waiting-observation.png`和`waiting-corridor-observation.png`：可确认隔断两侧不同入口及深色返回/切换按钮，按钮对比度问题已修正。仍存在明显取景问题：1.2m坐姿正对隔断，下半视野被1.05m矮墙占据，椅子在侧方；后续须改善首选观察方向/补充侧向观察证据，不能据两张截图签收完整空间关系。未构建APK，无Quest性能或实机视觉验收。

储存资源核查：OfficeRoom/ClinicalRoom节点名中未查到能明确确认的储镜柜；清洗室manifest含常规模型/贴图设备，但无足以证明柜内壁、通风、柜门及竖直悬挂的语义与拆件证据。不能仅据名字搜索宣称源模型绝无柜体，也不能把清洗设备或盘绕胃镜改名充数。ST-01柜内实物与ST-02连续登记仍未完成。

## 2026-09-21续作：候诊原模型派生与观察入口

默认R03_WAITING改为按需加载`FullScriptRooms/Waiting/WaitingRoom`，不再进入候诊程序方块布局。显式出版器`WaitingRoomPublish`从OfficeRoom原墙、楼板、天花板、灯和玻璃门烘出独立网格，保留灯/门原材质，地面复用图册V04；复用既有Chair独立网格三份。原后墙网格派生矮墙/透明实体隔断，原前墙/门复用为另一侧入口。逐项来源见发布目录`source-meshes.txt`；这是训练派生布局，不是实际医院扫描，不修改原FBX。

WT-01真实近触可收起检查面板，原位转头观察后返回原面板位置，不自动记查阅或答对。内容状态仍为RuntimeUnavailable：独立判断与完整情境尚未完成，不能把模型存在等同任务完成。

Android EditMode `artifacts/vr-interaction/waiting-source-room.xml` **36/36通过，0失败**，42.599秒；验证正式路线、原模型网格来源、三把椅子/隔断存在、观察返回姿态与未冒记进度。Vulkan主线截图`artifacts/stationary-final/waiting-observation.png`确实显示模型椅子、地面、隔断。人工检查发现椅子仍是办公室转椅，并非候诊排椅，视角不能充分证明两通道人流关系；这些仍须改善，尚未视觉验收。截图还暴露返回按钮对比度低，随后复用标准深色按钮样式修正；本截图和36项报告先于此样式修正，不冒称已重拍。三份椅子网格约47万三角形，Quest性能未实测。

没有新增生图，没有构建/安装/启动APK。储存库仍需消除开发布局；完整新稿实物、资料、判断仍未齐备，目标继续。

## 2026-09-21续作：正式prefab资源依赖隔离

正式`VisitorRuntime.prefab`通过Unity SerializedObject解除`_sceneLibrary`、`_contentEntries`、`_collectionCatalog`、`_physicalAugmentationCatalog`四项历史配置引用。当前VR绑定校验允许这些历史配置缺省；旧装配器若未显式提供历史配置则在分配模块前拒绝运行。历史测试/预览仅在`UNITY_EDITOR`中调用`ConfigureArchivedBindingsForEditor`，正式Awake没有恢复路径，旧源文件未删除。

递归依赖证据：`artifacts/stationary-entry/entry-dependencies-before.txt`为300项，after为220项，removed为80项。原`Content/Scenes`六站目录42项依赖降为0。75项项目资产逐个检查仍在磁盘；另5项为Package Manager逻辑路径。本证据针对正式prefab，不声称旧资源已从APK排除，也不是设备内存实测。

原prefab备份：`archive/visitor-entry-20260921/VisitorRuntime.before-dependency-isolation.prefab`，SHA256 `146E17B732599D3B0BD9359F153AEC5A84D17E376169D03A7757C165A6EC4BDD`。完整原稿/旧课资产继续保留。出版工具只显式执行、先检查递归依赖再回传prefab，没有构建回调。旧MR gateway出版菜单在VR配置下拒绝执行，避免回写旧MR目录（该新增防回退检查通过静态编译，未单独执行菜单）。

Android EditMode `artifacts/vr-interaction/stationary-entry-dependencies.xml` **43/43通过，0失败**，46.125秒。包含正式配置为空、旧课程资产不在prefab递归依赖、新主线全路线与汇总、显式历史兼容、生命周期、静音及旅程规则。20单元静态检查通过。无APK构建/安装/启动；储存/候诊场景及全量实物与资料仍未完成，目标继续。

## 2026-09-21续作：正式入口隔离旧课程

用户进一步明确：旧方案可归档但不接主线，完整剧本、无灰盒、资源实际消费仍是目标。此轮不是完成声明。

使用实际Visitor场景新增`ProductionStationaryEntryDoesNotCreateArchivedCourseModules`。红灯报告`artifacts/vr-interaction/stationary-legacy-red.xml`在大厅直接发现ModelController、VideoController、ImageRingFrontend、NarrationController，证实旧模块仍被初始化，非仅切房遗留。修复为`VisitorStationaryComposition`，正式原地入口提前分流，只装配原有手掌开场、真实手部注册、精灵/对话和新稿检查点；不调用旧课程/全景/答题/收藏/QR/步行导航工厂。旧工厂源码保留用于显式历史兼容回归。

正式汇总不再翻出旧P01–P06分页，也不再提示走到门口。新测试逐房检查旧模块不存在，并连续翻阅汇总检查旧课页未接入。`artifacts/vr-interaction/stationary-isolation.xml` **40/40通过**（41.973秒），包含新原位用例、历史流程兼容、真实近触对话、静音和旅程规则；静态20单元检查通过。

未完成项仍包括：Visitor序列化旧内容库的资源依赖尚需隔离；储存/候诊仍需正式建模与取景；最新稿实物交互、完整资料和判断没有齐备。没有把“不初始化旧控制器”等同于“旧资产依赖全部移除”，没有把测试通过等同于无灰盒或全剧本完成。README/spec/plan顶部已改为当前规则优先，早期描述仅留历史。

日期：2026-09-21 晚间。用户授权最新稿完全重构、旧内容保留、充分复用资源、允许生图；医院场地有限改为原地游玩。实际Unity工程仍为`app`，未构建/安装/启动APK。

## 为什么之前仍是灰盒/旧流程

运行入口Visitor已进入FullScriptJourneyRuntime，但RoomCatalog中R00没有高斯分支，落到FullScriptDevelopment。793729点派生高斯留在ImportedModels，只有编辑器预览加载；预览使用临时URP配置，生产renderer没有高斯feature。旧测试甚至要求开机不能加载大厅高斯，这条测试只适用于旧开发模式。

RequestRoom强制AtDoor；VisitorGuidanceCoordinator需要实际到站。带教洗消Map仍保留旧P01–06点位，外围房间主要是说明页，所以文档中的完整剧本并没有变成默认交互。这是运行绑定和流程实施缺口，不是用户没交资源。

## 本次实现

默认stationary=true，旧走动模式保留在兼容参数和既有测试中。主线复用Visitor手掌/精灵，但停止步行guidance.Tick，直接进入最新剧本目录。邀请使用独立主题副本，原主题不改，坐姿不受1.35m站姿下限限制。

房间切换支持原位手部确认；仍保留目的地锁、一次性转场票据、先卸载再加载、失败回退、复访、提交后冻结和重启清空。检查点切换新增FocusOut/Move/In，只在遮黑时将模型检查位对齐现实原位；XR头手不动、不缩放，阅读时不跟随。普通面板按实际眼高布置。

逐房目录保留办公室五组、储存三组、候诊、GI/RESP各四组、洗消六组。洗消排序RE-01、RE-02、RE-03、RE-06、RE-04、RE-05。办公室既有原图表格/六字段与模拟正文保持同源，可从目录原位打开。查阅细项和门体实际操作分别保存在本次内存，不能冒签未就绪任务。

大厅Prefab只引用一个用户高斯资产，进入大厅时加载，退出释放GPU/实例后再进入下一房。生产URP添加高斯feature；大厅临时管线MSAA=1，离开恢复原管线。嵌入MIT包新增显式双眼view/projection计算、逐眼slice绘制与合成，修正透明零alpha除法。原文件不改；源尺度暂按3倍视觉比例、源地面候选-1.525对齐，未声称实测米制或Quest性能通过。

门：源342041在清洗房manifest确实存在，对应已有拆分frame/leaf。原位缩小特写使用原网格，通过Meta旋转约束抓取把手，门角度≤5度持续0.4秒才记`RE-01:door-closed`；它不代替隔断、工位、分设等其他子项。原房静态网格和旧教学保留。

## 深查后的资源去向

| 资源 | 核查事实 | 实际用途/边界 |
| --- | --- | --- |
| 大厅两份PLY | 793729高斯点；另一个323602顶点/630285面彩色网格 | 高斯接默认入口；网格原件保留，不全量MeshCollider、不当第二房间加载 |
| OfficeRoom | 6100网格、原材质不缺但6250槽无底色纹理；已存在72批次派生房间 | 原派生房间继续使用，不重新全载BIM；电脑原图表格保留 |
| ClinicalRoom | 348网格、469415三角形；GI/RESP已有分室派生 | 按房独立实例，历史记录归属分别保留 |
| 原办公室椅子 | 采用已有办公室出版器已核查的同一座椅包围区 | 合并为独立Chair派生网格，候诊替换方块椅；候诊隔断为布局示意，非实地还原 |
| 342041门 | 源清洗室manifest与OfficeInteractions拆件ID一致 | 门体近景/把手旋转，源网格不改 |
| Gastroscope | 已有材质贴图、盘绕形态 | 原位抓取表面观察；绝不冒充竖直悬挂 |
| Disinfectant | 已有带标签模型与原材质、法定材料尚未核验 | 新稿产品近景复用，和办公室资料关联要求保留 |
| computer/table | 独立整理Prefab可用；办公室本身已有 | 派生资源可用，不在办公室重复堆一套家具，也不启动时全载 |
| 新稿image4/5 | 原图已在artifacts，哈希对应源稿 | 实际运行参考图，支持放大；PPE六部位逐页框示，仅图像教学 |
| 旧P01–06/全景/放大镜/视频 | 源代码和素材仍在 | 全部保留；不让旧流程替代新稿，不把旧成绩自动映射新任务 |

现有模型扫描未找到PPE医护人物。模板其他模型为精灵、植物、狐狸、魔法书及装饰，不能冒充医护教学模型。指定抖音页2026-09-21尝试访问失败，未获得可验证的4:46–5:06本地视频；不绕过访问限制、不拿通用片替代。

## 未完成边界

这不是“完整正式剧本已交付”：PPE三维人员、指定水视频、真实柜内/悬挂镜体、过滤器/水路/设备的全部语义、附件包装、正式报告和业务依据仍未齐。现在有目录、原图和能复用的真实资源，不将缺项计为学员跳过/正确。储存环境仍是开发结构，候诊是派生布局，不冒称源医院实景。

原地版从房间级切到检查点级，需要用户编译后验证真实触达、双眼深度/排序/遮挡、临时管线切换、每次单房内存和反复切换性能。编辑器截图不等于Quest验收。

## 验证记录

首轮Android EditMode：32项中29通过、3个新原地用例失败，原因为按钮事件内立即销毁近触PlaneSurface。修复为排队到下一次Tick重建，再重新执行针对性测试。最终具名结果和截图以本节后续记录为准，不将首轮失败隐藏成通过。

工具：`tools/publish_stationary_assets.ps1 -Preview`只进行Android目标资源出版与实际运行截图；`tools/test_vr_interaction.ps1`只进行Android EditMode。两者均不调用APK构建。

交付前一轮代码针对性验证：96/96通过，0失败，2026-09-21 11:33:51 UTC结束。包含新增6个StationaryScriptTests、旧FullScriptRuntimeTests、旅程/记录/追踪原点/观察交互/静音回归。实际Meta近触fixture验证全部房间在真实头手位置不变时可切换，旧走动模式显式以stationary=false保留回归。门体测试设置实际叶片角度验证判定逻辑，不声称已验收Quest抓取手感。

随后实际截图发现PPE原图的meta GUID误为33位，Unity忽略了该资源；修复为有效32位，并增加原稿两张图均可Resources.Load<Texture2D>的断言，再执行交付回归和实际截图。不是以文件存在替代运行加载验证。

修复后交付回归：`artifacts/vr-interaction/stationary-delivery.xml` **97/97通过，0失败**，耗时65.916秒，包含7个原地版新用例。原设备图及PPE原图均在真实Unity资源加载断言中通过。

Vulkan实际主线截图：`artifacts/stationary-final/`，15张覆盖大厅、逐房路线、办公室原表格、洗消六检查点。PPE/设备图实际显示已复核；日志未检出Shader error、Exception、C# error或无效GUID。截图检查后只将“放大原图”按钮从正文行移到细项行右侧，避免遮挡正文；此间距微调再做静态检查，不冒称截图已重拍。候诊仍为开发布局、视角/遮挡及整体视觉还需完善，不是已完成医院实景。

`tools/check_vr_static.ps1`通过20个源程序集检查及固定框架/路线针对性测试（旧TMP换行API保留警告）；`git diff --check`通过，只有既有换行规范提示。此次没有构建、安装或启动APK。
