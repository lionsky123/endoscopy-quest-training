# C08：完整复刻植物园工程，替换清洗消毒主题

> **2026-09-17 已由 [C09 VR 房间规格](VR_ROOM_C09.md) 覆盖环境与地图部分。** 当前启动直接进入 VR 房间，六点按房间过道重排，关闭主线 MR 透视、扫描、空间权限及标定入口。以下“保留原地图尺度/点位”“旧房间不加载”“MR 透视验收”均为 C08 历史记录；小精灵、六关内容、真实手追踪近触和主动换站继续沿用。

最新调整：第 1 关已改为房间全景观察、按需特写和 3 道巩固题，见 [第 1 关方案](FIRST_LESSON_ROOM_OBSERVATION.md)。后五关保留原样；下文未更新处为此前 C08 记录。

2026-09-16。根据用户“可以完全复刻那个项目，然后换个主题，关卡内容调整一下”的决定实施。本方案覆盖 C07 的局部复用和 CircleLesson 主线。

## 最新调整：去图鉴与地图召唤

按用户最新要求，运行主线取消图鉴、掌心书册/卷轴工具、手动地图召唤、工具教学及关卡后的折页入册。开场邀请和小精灵登场保留，随后直接进入带路；路线和目标自动可见，到站查看教学内容、确认观察或回答问题，再主动触碰出发。底层六点路线保持，导航就绪依据真实地面/玩家坐标，不再依赖打开地图。集合界面不绑定收藏事件且保持隐藏，完成学习和跳过答题都不会产生待收藏弹窗。

## 工程与流程

主工程仍为 `D:\quest3\EndoscopyBuild\app`，Unity 6000.3.23f1。参考工程为用户自己的 `D:\BotanicalGardenQR-Base`，只读复制，未修改参考工程。复制 Assets、Packages、ProjectSettings、MIT LICENSE 和地图编写数据，保留第三方声明；不复制源工程的 Library、Temp、用户设置、代理配置、平台凭据或 AGENTS.md。

采用参考工程的 Visitor 场景、OVR 手部配置、Oppy 小精灵模型和动作、对话界面、六点地图及曲线路线。沿用邀请册、掌心回应、小精灵登场、相遇对话、跟随可视路线、到站开启发现、查看内容、答题或确认观察、主动前往下一站的原流程。没有另写一套六站推进控制器。

保留原地图尺度和点位（scale=2，点位跨度约 10.6×10.1 米）。玩家真实走动，下一站由用户主动确认；按钮启动导航，不移动 XR Origin。旧清洗室房间模型不在新主线加载，其文件随旧工程归档保留。

旧工程备份：`D:\quest3\EndoscopyBuild\archive\app-before-template-C08-20260916`。其中包含 C07 和本次中途停止的局部复用修改，不作为最终版本。不要同时打开两份工程进行同一修改。

## 六关主题

| 原地图点 | 内容 | 素材 | 修复题数 |
|---|---|---|---:|
| P01 | 正确示范：布局与流程 | 正确图谱、清洗工位、用户全景 | 0 |
| P02 | 错乱案例：修复布局 | 错乱图谱、正确对照、清洗工位 | 3 |
| P03 | 正确示范：工具与水路 | 清洗、测漏、干燥参考图 | 0 |
| P04 | 错乱案例：修复工具水路 | 同主题参考图与案例问答 | 3 |
| P05 | 正确示范：产品与记录 | 标签、说明书、测漏参考图 | 0 |
| P06 | 错乱案例：修复产品与记录 | 同主题参考图与案例问答 | 4 |

继续使用已有课程的题目、正确答案、解释和重试提示，共 10 题。沿用模板内部场景 ID（giant_saguaro、baobab、bottle_tree、ceiba、macrozamia、welwitschia），它们只是稳定的绑定键；对用户显示清洗消毒标题。原植物模型、视频、讲解和增强点没有接入这六关。

素材原件在 `app/Assets/EndoscopyTheme/Media`，各关运行素材按原模板约束复制到 `app/Assets/BotanicalGardenQR/Content/Scenes/<场景ID>/Endoscopy`，用户全景已复制入工程，不依赖微信临时目录。参考图仅支持观察，不能据图片外观证明设备、水路或记录已经合规。图谱标注为教学示意/模拟案例。本轮没有导入新视频，亦没有把植物视频包装为医疗视频；素材来源沿用 C05 素材记录。

## 必要适配

- 全程静音：AudioManager 禁用音频。模板图像环廊要求每张图具有独立音频资源，配置为三个不同的零采样音频，不恢复语音、音乐或按钮音效。
- 所有学习面板采用真实手部 Poke 近触。保留模板已有的对话 Poke；原凝视面板接入同一套真实 OVR PokeInteractor 的表面事件，持续按住不重复提交。Button 子类屏蔽 PointerClick、PointerDown、PointerUp 和 Submit。没有加入鼠标、键盘、射线或手柄提交入口。
- 对话/答题初始距离约 0.55 米，图片环廊约 0.60–0.65 米，全景返回控件约 0.50 米；保持原界面构成并按近触距离调整尺寸。相应提示改为食指触碰。
- 保持对话、内容与问答之间的输入暂停语义，模态界面占用时不会误提交背景面板。
- 包名 `com.endoscopy.inspection`，显示名“内镜中心监督检查”，源码版本 0.9.0 / C08；Android versionCode 最低 14，用户显式构建时递增并生成 `.receipt.json`。不继承源工程签名凭据。保留模板空间标定 Admin 场景，但仍只打包一个中文应用。工程准备和用户显式构建前排除 Meta 自动生成的 DevAgent 配置，保留原模板的发布拦截检查；未调用构建验证。
- Android OpenXR 的 Composition Layers Support 已启用，工程加载配置和显式构建前保持此设置。这些设置代码不调用 BuildPlayer。

## 验证与边界

本次去图鉴调整：2026-09-16 18:59（北京时间），Android 目标的外部隔离工程 EditMode 测试 5/5 通过，报告 `artifacts/replica-c08-no-atlas-tests.xml`。覆盖六站无图鉴/无待收藏弹窗完成、跳过答题分支、主动离站、路线/目标不受内容根节点隐藏影响，以及既有完成去重行为。未运行 APK 构建，未修改参考工程；不是头显实机验收。

初次复刻的历史验证（早于本次去图鉴调整）：`artifacts/replica-c08-tests.xml`：2026-09-16 18:22:59（北京时间），7 项针对性 EditMode 测试通过，覆盖六关实际发布素材和 10 道题、中文应用身份和静音、真实 Meta Poke 表面快速穿越/保持/取消、非近触入口拒绝、原模板完整六站推进及主动离站、原对话提交门控，以及模板完整只读架构校验。

素材迁移中曾出现 Unity managed-reference 空条目序列化格式异常，导致整个引用表无法正确加载。通过 Unity 编辑器配置接口重写素材，并恢复 Unity 的空类型条目格式；关闭再重新打开工程后，内容完整性测试通过。保留显式内容导入工具 `Assets/EndoscopyTheme/Editor/ClinicalContentImport.cs`，不在启动或构建时自动重写课程。

界面预览使用模板原有编辑器渲染工具，输出在 `artifacts/replica-c08-preview`。检查清洗消毒题目和模板对话布局；编辑器背景不是头显 MR 透视效果。原模板的若干预览夹具仍用于独立 UI 展示，不能把全部截图当作运行路线记录。

本机无完整桌面 XR 运行环境，进入 PlayMode 时模板 MRUK 原生初始化失败（含非法 UTF-8 日志转换与 context 创建错误）。本轮采用不启动原生 XR 的 EditMode 测试和界面渲染；不将它们当作头显端整流程验收。

没有构建 APK、安装或启动 Quest 应用。设备中已安装的中文旧版不会因为源码替换而自动更新。仍需用户自行编译后在头显确认真实手追踪、MR 透视、实际场地和整条六站体验。

## 后续修复：保持 Quest 构建目标

用户明确两份工程均为 Quest 工程。本次在内镜工程和参考工程加入现有 Meta Quest profile 的打开时选择逻辑；相关 Unity 检查/素材处理脚本显式传入 -buildTarget Android，移除旧内镜脚本的 Desktop 入口。未修改包名、画质或场景参数，未构建 APK。内镜运行中的 Editor 已只读核实 target=Android、active profile=Meta Quest。参考工程未打开，其自动选择逻辑将在下次打开时生效；启动参考 Editor 的即时核验被自动审批拦截（仅返回 blocked by policy）。PowerShell 语法检查通过。此处平台修复是对此前‘参考工程只读’约定的用户明确授权例外。
