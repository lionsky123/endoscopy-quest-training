# Agent Skills 工作流整理记录

日期：2026-09-23。范围：项目文档、规则和阅读入口；本次不实施 Unity 业务、资源、配置或设备变更。

## 目标与验收

将已有项目接入 Agent Skills 的规格、设计与任务规划流程，面向 GPT-6 使用标准 Markdown 和分层 AGENTS.md。完整产品范围、已有代码、原计划任务编号及验收门槛保持不变。

- [x] W01 明确工作流和权威文档。已写入 [工作流](WORKFLOW.md)、[导航](README.md)与根规则；沿用原规格和合并计划，tasks/plan.md 只作索引。已对照确认未创建第二份业务任务状态。
- [x] W02 整理根目录阅读入口与 Unity 补充规则。根 README/SPEC/EXECUTION_PLAN 已改为现行导航，原文保存在[归档](history/workflow-before-20260923/README.md)；旧 grill-with-docs 强制前置已原位替换。已对照现行入口确认单一引导、原地交互与构建边界一致。
- [x] W03 检查文档链接、差异和状态保全。9 份现行入口/规则/工作流共 66 个本地链接均可解析；修改文件的 git diff --check 通过；3 份归档反向还原链接后与整理前正文一致。本轮未修改业务勾选或业务代码；并行变化按下方记录保留。

## 后续规则精简

- [x] W04 审查并精简根/app AGENTS.md：根文件保留公共规则，app 只保留 Unity 实现要求；工作流引用根文件的验收分工。两份规则正文合计约 8,097 → 4,373 字符（减少约46%），分别为67/26行。移除重复产品/UI/验证条款、历史工作流名称及冗长示例；将“用户自行编译”明确为“用户自行构建 APK”。逐项对照保留完整范围、全景大厅、真实手部、编译门槛、按需渲染、任务勾选和授权边界。4份相关文档20个本地链接有效，根/app git diff --check通过，写入内容回读一致。本轮仅修改这4份Markdown，未运行Unity或修改业务验收状态。

## 初次迁移依据与取舍

- 当前环境已提供 `agent-skills@agent-skills` 0.6.10 的技能；直接使用，不重复安装或复制插件到仓库。
- 使用 `spec-driven-development`、`planning-and-task-breakdown`、`context-engineering`；已有完整规格与任务载体继续沿用。
- 用户已授权本轮文档整理；不把技能默认的逐阶段人工审阅转换为重复确认。本轮完成文档，不由此启动旧计划的业务实施。
- GPT-6 适配采用精确边界、按需读取、保留完成条件和已授权工作连续推进；不更改模型设置，也不声称完成模型运行评测。
- 用户追加并澄清验证规则：代码、程序集、依赖或编译相关配置变更必须由执行者确认 Android 目标下 Unity 编译通过并处理报错；完整原地流程、真实 Play 画面和实际体验由用户自行验收，报告 Bug 或明确要求时再检查相关路径。减少重复渲染不等于省略编译检查。已原位同步根/app 规则、工作流与合并计划入口。
- 官方依据：[GPT-6 Astra 技能与提示整理建议](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)、[AGENTS.md 层级说明](https://learn.chatgpt.com/docs/agent-configuration/agents-md)。

## 验证记录

- 本轮写入仅为 Markdown：根/app 规则、根阅读入口、工作流/导航/兼容索引、迁移归档，以及原计划和状态页的入口说明。未运行 Unity、APK 构建、渲染或设备操作；纯文档变更不声称验证了当前工程编译。
- 静态检查：9 份现行文档、66 个相对链接，无缺失目标；所改已有文档 git diff --check 无错误（Git 原有换行提示不属于编译/验证证据）。
- 归档校验：README、SPEC、EXECUTION_PLAN 的整理前正文完整保留，仅加历史身份说明并转换相对链接；反向转换后的正文比较通过，历史未完成事项未改写。
- 整理期间检测到工作区有其他并行变化：根规则的大厅要求由高斯改为用户全景，合并计划新增全景替换任务，app/tools 差异哈希也发生变化。这些不是本轮文档整理实施的代码修改，已保留；因此不声称整个工作区与开始时完全一致，也不把并行任务的勾选算作本轮成果。
- 本文件勾选仅表示文档整理完成，不签收 M1–M5、C01–C11、当前编译或 Quest 验收。后续接续业务任务仍按实际源码及原计划证据处理。
