# 场景图与全景或高斯模型交接

最新入口：[带图片的完整资产图册与接入台账](ASSET_CATALOG.md)。生成图应是项目实际展示的界面/资料/教学素材，并记录实际消费者；3D场景渲染和概念构图不作应用画面。2026-09-23已清理5张过时、未使用的图像，文件哈希、原因和替代关系见[图像清理记录](experience-20260923/IMAGE_CLEANUP_20260923.md)。原剧本图、用户提供的大​​厅全景、当前运行资源和运行截图保留。

2026-09-21。按用户最新要求，参照0920剧本文档图片调用内置imagegen生成，不使用CLI/API回退。场景图片用于后续全景/模型生成；不是实地照片、360成品或已验证的可走三维模型。

## 历史制作范围（2026-09-21）

以下为2026-09-21的历史记录，不构成当前模型导入授权；现行规则限定模型来源为`C:\Users\i1204\Desktop\Models`。

当时只读核对了`app/_IncomingModels/Models.zip`目录，确认有办公室、诊疗室、computer、table、胃镜、消毒剂、清洗室和门的网格。

- 用户目前只需生成大厅高斯场景小样，不需要同时生成360全景。
- 办公室参考图保留作视觉参考，取消办公室高斯制作请求；办公室与诊疗室优先整理现有FBX，核查布局、材质、单位、节点数量和性能后接入。
- 清洗室沿用项目现有处理版；带标签消毒剂已由`course.json`用于P05，不重复导入。胃镜与门拆分网格列为优先复用资产，但尚未由本轮接入。
- 电脑和桌子据原审查缺少外部贴图，先保留几何并检查/补齐材质，不视为完整外观成品。
- 包内没有单独命名的大厅、储存库、候诊区模型；房间内部能否拆出后两者所需家具待检查，不立即要求用户补生成。

后续用户已授权先导入：本轮现已导入办公室、诊疗室、电脑、桌子、胃镜与门拆件，并完成Unity预览及统计，见`../FULL_SCRIPT_ASSET_GAPS.md`。新模型尚未替换运行灰盒，未完成Quest性能验收。最新spec/plan为v2，明确只按当前房间加载，不一口气全载。大厅之外的既有房间不再要求用户重复生成。

## 已清理的旧场景参考图

此前生成的大厅和办公室环境图仅为透视场景参考，没有应用运行消费者；已标记退役并从活动文档移除引用，文件仍待清理。大厅使用用户提供的360°全景，办公室按用户指定目录内的模型与原始材质核对，文档里的系统界面/记录素材按真实运行消费者接入。

先生成大厅高斯小样，确认接入后再决定缺失场景是否需要生成，不批量制作办公室等已有模型的场景。生成时保留参考图的结构、家具比例和主要位置；室内有站立与步行空间。高斯泼溅请提供原始模型文件、生成软件和导出格式；若能导出网格/碰撞模型也一并提供，并说明单位或一个已知尺寸。

当前工程未检出已配置的高斯泼溅导入/渲染模块，不能承诺任意格式直接加载。收到小样后再确认格式、坐标、材质、性能和碰撞。单图没有提供背面与遮挡区域的实测信息，生成工具补出的部分需要检查，不视为原医院实测布局。

360用于观察场景/远景；不能单独证明房间内实际步行、视差和物体交互已经实现。高斯场景也必须核查导航和碰撞，不以“能显示”冒充可走验收。保留真实手部、静音、固定世界和用户自行编译要求。

图中制度牌、记录本封面和小字号电脑文字是视觉示意，不能当作正式医疗/法律证据；正式表格和业务文字后续由可读、可核验的内容层提供。

## 仍在使用或待接入的配套图像

保存在`app/Assets/EndoscopyTheme/Resources/ClinicalCourse/FullScriptVisuals/`：

- `office-records-v1.png`：旧版带五行烘焙示例数据的追溯屏幕图，已无运行消费者，标记退役但文件待清理；正式运行改用空白表格纹理 `office-records-reference-v2.png` 叠加同源程序数据。
- `hospital-terrazzo-v1.png`：浅灰细粒水磨石底色纹理。
- `office-oak-v1.png`：浅橡木办公桌底色纹理。

当前运行使用`office-records-reference-v2.png`作为按文档image3加工的空表格，由程序叠加同源数据。实际运行截图与23/23回归证据见`ASSET_CATALOG.md`。旧版`office-records-v1.png`已退役但文件待清理。其余家具/设备材质和光照未完成，不把局部接入说成完整写实成品。

完整生图提示词见`PROMPTS.md`，参考图从用户DOCX的`word/media/`原样提取到`artifacts/full-script-reference-images/`，未改写原文档。
