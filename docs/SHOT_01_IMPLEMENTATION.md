> 当前有效设计为 **C05：MR 桌面展台 + 小精灵实际带路 + 六圈问答**。用户已明确覆盖此前的禁止答题/光圈及关门、搬物主线。参见 [C05 设计与实现](CIRCLE_LEARNING_DESIGN.md)。以下 M04 及更早内容为历史记录，不能当作当前入口。未构建或安装新版 APK。
# 镜头一现场核查实现记录

> 最新 M04 覆盖：取消先开后关及示教盒搬运，改为合规全景后的单次关门纠正、设备关系观察和单向演示。默认 app 已按用户授权卸载，中文包保留但未更新。源码 0.8.1，新包由用户自行编译；以下 M03/面板版本内容仅作历史。当前规则见 [M04 教学设计](SHOT_01_TEACHING_DESIGN.md)。

> 最新状态（2026-09-16）：用户反馈模型仍不能互动、关卡设计奇怪。已查明设备中文包是旧面板版，本地模型版未安装；当前关卡退回设计复核，不能再称为已完成/已认可。源码新增 0.8.1 / code 10 与 M03 原型标识，设备及现存 APK 未改。详见 [设备排查记录](DEVICE_INTERACTION_DIAGNOSIS.md)。

## 最新修订：模型操作理解（2026-09-16）

用户明确否定主要在面板上作答，并选择模型操作理解。本版 LayoutLesson 删除全部答案和提交入口，完成来源改为模型事件。门按实际把手抓握后的手部位移旋转，打开再关闭并松手才完成；LayoutModelTask 新增封闭示教盒的直接抓取、专用设备旁归位、终末漂洗至干燥单次交接。错误位置保留可见供重新拿起纠正；追踪丢失不计完成。对话只负责短引导和主动换站。

示教盒与待处理台为本项目新增教学模型，盒内不声称存在已验证的真实内镜资产。它们表达设备分设和流向，不替代临床操作规程。先合规展示，再允许实际操作造成并纠正错误，不再通过随机题目制造面板答题。

静态检查通过；更新后的定向 Unity 测试 1 项通过，验证零答案按钮、把手开合、追踪丢失不计完成、两种模型错放及重新拿起纠正、正确台面松手完成与下一站接续。结果为 `artifacts/layout-model-tests.xml`。`04-model-*` 为对应截图，`workspace` 截图仅在测试中模拟低头看台面，正式运行不强制俯仰视角。根据截图将无按钮提示缩成上方短条，修正示教盒与槽沿的高度关系；提示不再要求点击面板。Quest 实际抓握、碰撞和触达尚未验证。下方三次面板判断方案及更早多轮核查均为历史，已被本节和最新教学设计替代。

## 历史修订记录

## 最新修订：分步带到门前，缩短主线（2026-09-16）

用户反馈“找不到对应的门、检查太多、流程冗余”。已重写 LayoutLesson：入口和带看每页一个下一步，问题页只有两个直接结论。取消对象列表、确认提交、每主题两项两轮及错误后额外复查；主线由原十二次判断压缩成门、机器、流向三次。错误原位一句说明并重选，正确自动恢复合规，再主动确认换位。完成记录明确为引导练习，不代表六项独立考核。

入口按钮“站稳后 · 带我到门前”直接到正对目标门的位置；门上新增“当前目标 · 这扇门”文字和跟随把手的金色引线，正常与异常状态都显示。引线采用无碰撞体交互的细条标记，直接读取把手 Transform，在提示启用时立即更新，避免等待物理同步导致初始定位缺失。Oppy 站到门旁朝门，保留真实把手食指开合。没有增加跨房间追随路线或自动头显移动。每次门/流程动画结束后再接受判断。

当前脚本与验收以 [教学设计](SHOT_01_TEACHING_DESIGN.md) 为准。下方保留前一版实现和测试的历史记录；其中多轮独立复查、对象列表、确认提交等流程已被本节替代，不是当前需求。

当前验证：Runtime/测试源码静态检查通过；160 项原有领域/把手断言、资源 GUID 闭包通过。更新后的 `FirstShotSceneDoorGuideAndEvidence` 定向测试 1 项通过，覆盖门前视野、定位标记、门扇独立、每页选项数、一次错误原位纠正、三次判断和下一站接续。结果为 `artifacts/layout-short-tests.xml`，对应编辑器单眼截图为 `artifacts/layout-preview/02-guided-door.png` 等。未执行全套测试或 Quest 佩戴验证，未构建 APK。

## 前一版实现与验证记录

更新：2026-09-16。范围：清洗消毒室镜头一。用户批准从设计进入优化实现，后续明确希望门可以移动；本轮直接在原门洞内制作独立门扇和铰链，不需要网上另找门或拆出独立关卡。

状态：源码和资源已接入；静态检查、纯逻辑测试完成。继续执行后新增镜头一定向 Unity 编辑器测试 1 项通过，并核对正式教学相机截图。未构建 APK、安装或启动 Quest；完整视觉与佩戴验收未完成，项目版本号和现存 APK 均不更改。

## 继续执行：编辑器验证与构图修正

- 新增 `FirstShotSceneDoorGuideAndEvidence`，加载实际 `Inspection.unity`，检查 Oppy/Animator/材质可用、门扇 68° 开合而门框固定、演示不计掌握、六项判定及第二站接续、静音。使用模拟食指触碰，不代表真实追踪测试。
- 首次检查发现临时测试场景的默认相机污染截图。现明确加载正式入口，校验 `Camera.main` 为教学相机，截图显式使用 `InspectionApp.TeachingCamera`；未在生产入口添加通用禁用相机逻辑。
- 增加房间全景观察位 `(6.5,0,-2.35)`，可先收起对话环顾；用户主动确认后前往门与隔断。没有自动凝视计时或强推视角。
- 完整对话框由低于眼位 0.25m 调整为 0.10m，紧凑提示为 0.20m；字幕与按钮在检查截图中完整显示。面板位置在观察位内固定，不持续跟头。
- 机器观察位后退至 `(2.9,0,0.85)`；现场标签缩放从 0.001 调整为 0.00065，避免铭牌占据大半视野。精灵横向偏移缩小到 0.45m，减少与相邻设施交叠。
- 统一迁移着色器换行，清除导入时的混合换行警告。编辑器中角色材质支持检查通过，未证明 Quest 双眼呈现。
- 最新定向测试：1 项通过，0 失败，结果 `artifacts/layout-unity-tests.xml`，日志 `artifacts/layout-unity-tests.log`。仅运行这一新增用例，没有运行全套旧测试。未调用 Prepare/BuildAndroid/BuildAndRun 或 Android 构建入口。
- 已查看 `layout-preview/01-welcome.png`、`01-room-overview.png`、`02-door-open.png` 及三个主题画面；这是编辑器单眼渲染，不能替代 Quest 阅读距离、触达、场地边界及舒适度验证。

## 当前入口与体验

`InspectionApp` 加载原房间后，将房间引用交给 `HandsOnLesson`，默认先进入 `LayoutLesson`。三个主题对应原有 CL-SEPARATE、CL-SYSTEM、CL-STAGES，全部通过后接续第二站。第一关原有的球、分类名牌投放和依次碰五个流程点已退出代码入口，分类名牌方法已删除。

入口 → 小精灵欢迎 → 合规说明/收起看现场 → 选择现场对象 → 起草结论 → 明确提交 → 针对证据反馈 → 恢复合规 → 不同状态的独立复查 → 主动确认下一观察位。每个主题分别检查两项条件，共门、隔断、槽、机器、环节、流向六项。观察和选择对象不会计完成。

每主题安排合规与一个异常的两种状态，顺序变化。错误或提示后可以纠正，但必须再核查不同状态，不能凭提示后的原题答案记为独立掌握。选择对象支持实际表面触碰和近距离对象列表；必要时主动确认到对应观察位。流程动画可以重看、暂停，用户不需要拿取动画对象。

## 可开合的门

- 原门洞位于北墙 `[342041]`。不改原 FBX/导出网格文件，镜头一临时隐藏原来合并的门和北墙网格。
- 使用原墙范围与门洞位置重建教学状态层，门框固定、门扇独立，围绕左侧铰链在 0–68° 之间平滑转动，速度为 110°/秒。
- 合规示范中可用食指近距离触碰实际把手，或触碰“查看门开合”控件查看；没有捏合点。手离开把手后才能再次触发，运动中不接受重复切换。
- 独立核查时锁定情境中的目标门状态；门仍在运动时不接受选择/提交。触碰门用于指认，不能通过关门直接完成核查。
- 门观察位后退到 `(-5.76, 0, 1.95)`，避免对话面与门扇扫掠区重合；精灵放在该观察位左侧，避开右侧干燥台。
- 退出镜头一后恢复原对象的启用状态。门框、门扇细节、开口背景与人体实际触达仍需 Unity/Quest 视觉验证。

## 现场对象与情境

| 检查对象 | 来源与映射 | 可见异常 |
|---|---|---|
| 门与隔断 | 原北墙 `[339793]`、门洞 `[342041]` 的运行副本；补建固定框、门扇、把手及墙体开口状态 | 门开启；隔断出现实际开口 |
| 消化槽组 | 原 `tripo_node_51f4bd56/057/058/059` | 与撤去呼吸槽组配合，呈现共用用途 |
| 呼吸槽组 | 原 `tripo_node_51f4bd064/063/062/061` | 对应独立槽组撤去，原配置与共用状态可对照 |
| 两类清洗消毒机 | 原房间内新增教学机体，包含机门、观察窗、控制面板、把手与底座 | 撤去呼吸专用机，另一台实际承担两类用途 |
| 五环节 | 四个原槽按教学流程映射，加独立干燥台 | 终末漂洗对应原槽实际隐藏，位置仍可选中核查 |
| 交接方向 | 连接实际工位位置的逐段轨迹、移动方向箭头和当前交接段文字 | 在终末漂洗后加入返回漂洗的路径 |

这套分组和新增设备是明确的教学配置，不宣称还原原 FBX 的临床用途或真实医院安排。原模型的其他槽列和设施保留为背景；实际用途映射、设备外观精度、管路细节与完整甲方画面仍须业务/视觉核对。缺环节与回流分别判定：缺终末漂洗时交接到干燥，不凭缺件额外声称发生了回流。

同一个 `LayoutCase` 同时驱动 `LayoutRoom.Apply` 和 `LayoutAttempt`，没有独立的隐藏答案变量。对共用设备的判断必须指出实际共用设备，不能指着缺位处也算找到问题；回流必须选择返回的那一段。

## Oppy 与对话资源

参考工程：`D:\BotanicalGardenQR-Base`（只读），HEAD `11e8245edf7b7b6fa5d9c57c399c822b79d35fe8`；实际文件来自当时工作树，不宣称工作树所有资源已提交。双方 ProjectVersion 均为 Unity 6000.3.23f1，目标工程 URP 为 17.3.0。

导入目录：`app/Assets/Endoscopy/Resources/Guide/Oppy/`。复制该选定资源目录的模型/Avatar、现有动画 Controller 与片段、材质/贴图、肖像、着色器和许可/来源说明，保留原 GUID。初始复制 56 个文件共约 275.5 MiB，主要为源 FBX，不能据此推算 APK 体积。未复制花园脚本、场景、Packages、音频或 Library。

上游为 Meta Unity-TheWorldBeyond，来源修订及 MIT 许可保留在导入目录 `NOTICE.md` 与 `LICENSE-Meta-TheWorldBeyond.txt`。本工程移除了 ToonFoggyOppy 对花园 ArrivalBoundary.hlsl 的 include 和裂隙裁剪代码，保留 URP 着色及双眼渲染声明；本轮已通过 Unity 编辑器导入及材质支持检查，尚未验证 Quest 渲染。

`InspectionGuide` 采用深墨绿、细金边、米白字、左侧肖像和右侧短正文，按钮全部使用现有 PokeOnlyButton/HandButton。正文一次显示，支持收起为当前任务；不导入参考的凝视模式、语音或确认音。角色使用站立、挥手、点赞、好奇表现，无音源；当前随主动观察位切换重定位，尚未加入场内行走带路，也未把精确指向动作冒充已存在的动画。

## 检查证据与范围

前一阶段执行 `tools/check_layout_static.ps1`，仅使用本机 Roslyn 和 .NET，保留结果如下；新增 Unity 定向验证见本文开头：

- Runtime 全部 C# 源码静态类型检查通过；原 WashingRoomAsset 的 JSON 反序列化字段产生已有未赋值警告。
- 160 项独立断言通过，覆盖正确对象/结论、错对象、误报、重复提交、跨主题提交、提示、纠错和尝试重置、缺环节不等于回流，以及把手触碰的离开再接近、运动防连触和追踪丢失重置。
- Oppy prefab/controller/material/meta 的 GUID 引用闭包通过，未留下花园专用着色器 include。
- 已更新 `HandsOnTests` 的默认入口回归：检查门扇转动但门框固定、开门不计掌握，再通过食指触碰完成第一关并接续其余八项旧动作；新增 `LayoutInspectionTests`。这两份测试脚本的静态类型检查通过，本轮不执行 Unity 编辑器测试，也不沿用旧 30 项报告冒充新结果。

待佩戴/视觉核对：原槽语义和题目辨识度、缺位与备用背景槽的区别、门开合碰撞与可达距离、动画与材质、深色对话的双眼阅读、对象轮廓是否遮挡证据、前后切换的舒适度和整体验证。场地边界仍由实际使用条件决定，本轮未验证空间安全配置。

本轮只优化第一关。后五站仍有旧球/卡片动作，已在计划中列为待替换；没有把它们宣称为新教学方案或本轮验收完成内容。








