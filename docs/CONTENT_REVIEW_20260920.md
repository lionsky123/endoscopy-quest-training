# C09 内容与实现审查（2026-09-20）

审查范围：当前工作树，基线 HEAD `2b4bc3b`，包括此前已有的未提交及未跟踪 C09 源码。用户要求“审查梳理一次内容，整体自动优化”，本轮直接修复明确问题，不改成尚未确认的新课程设计，不构建或部署 APK。

## 需求一致性

已确认主线采用真实手部，无凝视提交/准星/无手后备；P01 放大镜三处观察、零答题；P02 排序和处置两项；后五关多媒体、主动离站和实际步行均已接入。固定世界、静音和 Android 目标检查通过。

发现 **1 项 P2 缺口，已修复**：`VisitorHandReadinessAdapter` 在开场完成后停止采样，后续丢失手跟踪没有恢复说明，未满足 `VR_HAND_FIXED_C09.md` 的“没有手部跟踪时提示将双手放到视野前方”。现在初遇/探索持续丢手 2 秒后显示短提示；手恢复后立即收起，位置在出现时固定。原点恢复停用交互时不叠加手提示。

内容整理：当前合计 12 项，明确区分 3 项引导观察与 9 项核查任务；媒体与虚构情境标识保持原样，不新增临床结论。README/STATUS 不再混列多版“当前”体验；旧状态全文保存在 `STATUS_HISTORY_20260918.md`，六关规则集中在 `CURRENT_EXPERIENCE_C09.md`。

## 实现质量

发现 **3 项 P2 行为问题，已修复**：

1. `ClinicalCoursePanel` 视频准备完成仅检查可见性。准备期间正确提交后仍可能开播；隐藏期间完成准备则丢失纹理绑定和计时清理。现统一保存准备结果，实际播放须满足当前任务、可见、仍有播放请求；失败后迟到的回调无效。反馈阶段禁用媒体按钮，隐藏不保留自动播放请求。
2. 同一面板不检查真实握持状态就回收超距瓶体，“翻看 / 取回”也会抢占另一手持有的位置。现读取 SDK 的实际选择状态，握持期间两种回收都不执行；松手后继续回收。兼容未运行 Awake 的编辑器预览对象。
3. `ClinicalObservationToken` 把冷却期 Select 丢弃，但 SDK 已开始抓取，导致错误自动回收和跳过判定。现将真实握持与进度资格分开；冷却期开始的抓取保持真实握持，但不会因等待冷却结束而补计完成。

没有为风格进行大范围重构，没有删除旧兼容题库、第三方素材或未完成设计。模型初始化中去掉未使用的边界遍历；不以此声称帧率已有可测提升。

## 验证工具

新增 `tools/check_course_content.py`，验证当前内容结构、六站绑定、任务数量/模式、证据页、排序索引、媒体路径/.meta 与来源文件。

修复 `tools/test_vr_interaction.ps1` 的结果判定：运行名约束为单个文件名；启动前清理同名旧结果；正常进程退出仍须有新生成、非空且全部通过的 XML。避免残留文件或未实际执行测试时误报通过。

更新旧集合兼容性回归：该测试消费完成事实，不再要求每个站点有旧文字题。当前六站主动离站仍由独立主线测试覆盖。

## 验证结果与边界

- 初轮 `content-review-20260920.xml`：73/77，通过回归发现编辑器抓取状态未初始化、EditMode 停用回调需显式驱动、旧集合测试仍要求文字题存在。报告保留，未作为最终通过证据。
- 最终 `artifacts/vr-interaction/content-review-20260920-final.xml`：**77/77，20.6205551 秒**，Android 目标 EditMode，进程退出码 0。
- 新增视频边界测试 3 项、放大镜冷却抓取 2 项、手跟踪提示 3 项；已有瓶体实际 SDK Poke 测试扩展真实握持/回收检查。
- 六关内容检查、VR 资源检查通过。审查基线 18 个 Roslyn C# 单元及固定六站路线通过；最终改动经 Unity 脚本编译与上述回归验证。
- 未构建 APK、未安装或启动 Quest、未进行本轮设备核验。未把历史截图当成本轮视觉验收；实际手识别、舒适度和边界切换仍待用户编译后真机验证。

两条审查轴分别为：需求一致性 1 项已修复；实现质量 3 项已修复。文档和验证工具改进另列，不扩大为全工程零缺陷结论。
