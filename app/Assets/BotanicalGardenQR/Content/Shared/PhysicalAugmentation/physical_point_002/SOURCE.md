# physical_point_002 test content

This temporary bottle-tree test Point uses the independently imported Khronos Fox GLB and its `Survey` animation through the generic
PhysicalAugmentation Legacy Animation adapter. Its performance is intentionally replaceable through configuration and contains no
bottle-tree-specific runtime code. Source revision, file digest, attribution and licenses are recorded in `ThirdParty/Fox-NOTICE.md`.

The simple depth-only trunk/crown proxy is a development approximation. It requires Quest multi-view calibration before any commercial claim.
