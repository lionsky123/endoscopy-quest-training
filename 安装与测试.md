# Quest 3安装与测试

默认由用户自行编译；助手修改代码或配置后仅做必要检查，不自动编译 APK 或安装到设备。

## 渲染管线构建错误修复

### TraceVirtualOffset 黄色精度警告

已核查 SRP Core 17.3.0 的包源码：`TraceVirtualOffset` 位于 `Editor/Lighting/ProbeVolume/VirtualOffset`，用于光照探针的虚拟偏移计算。`Sampling.hlsl` 中浮点计算结果转换到 `min16float` 时，D3D11 编译器报告潜在精度损失。当前 ClinicalPipeline 的 `m_LightProbeSystem` 为 0（LegacyLightProbes），未启用 ProbeVolumes；Quest Android 使用 Vulkan。这条 warning 本身不是构建失败错误，目前无需为消除此提示修改 PackageCache 或升级渲染包。此结论仅针对这类 TraceVirtualOffset 精度警告，不代表所有黄色警告都可忽略。

2026-09-16 补充：原生构建入口现在也会保存 Quest 图形配置为 **Linear 色彩空间、关闭 Auto Graphics API、Android 使用 Vulkan**，修复 Gamma/OpenGLES 的 OpenXR 校验错误。这些设置与项目菜单共用同一配置方法，并在编辑器脚本载入时应用。已执行 OpenXR 原生校验，阻塞错误为 0；记录见 artifacts/quest-graphics-validation.txt。此次验证没有编译 APK。

若旧配置出现“Quality levels and Graphics Settings ... different render pipelines”，原因是默认 Graphics 管线为空、部分 Quality 等级单独指定 URP。本项目现已将默认 Graphics 和全部 Quality 等级统一保存为 ClinicalPipeline，并在 Unity 原生 Build / Build And Run 入口应用相同配置。

等待 Unity 完成脚本刷新后可自行构建；也可通过 **Endoscopy > Repair Render Pipeline Settings** 单独修复设置。此菜单仅修复配置，不编译 APK。针对原错误的 Unity 原生检查回归验证已通过，记录见 artifacts/render-pipeline-verification.txt。

## 文件夹怎么选

- **主项目：D:\quest3\内镜中心VR监督检查项目**。源码、资料、计划和安装包全部在这里。
- **Unity工程：主项目内的app**，其中包含Assets、Packages、ProjectSettings。
- **D:\quest3\EndoscopyBuild** 是指向同一个主项目的目录联接。Android构建不能使用中文路径，所以Unity构建推荐打开 **D:\quest3\EndoscopyBuild\app**。它与中文目录的app是同一份文件，不要把两个入口当成两套工程分别维护。

Unity版本：6000.3.23f1。通过tools/build.ps1构建时会自动验证英文联接。

**推荐直接双击D:\quest3\Open-Endoscopy-Unity.cmd打开工程。** 如果Unity当前打开的是中文总目录，先保存改动并关闭该窗口。在Unity Hub中添加已有工程时，请精确选择 **D:\quest3\EndoscopyBuild\app**；漏掉app会打开错误的工程层级，使用中文路径会导致Android构建报Invalid project path。英文入口已经存在，无需重新复制项目。

## 自己编译到Quest

1. 保存并关闭误开的中文总目录工程，双击 **D:\quest3\Open-Endoscopy-Unity.cmd**。加载后应能看到顶部 **Endoscopy** 菜单，以及Assets/Endoscopy里的代码和场景。
2. 在Project面板打开 **Assets/Endoscopy/Scenes/Inspection.unity**，这是本项目主场景。编辑后的场景正常保存，构建不会再重建覆盖已有主场景。
3. 只编译安装包：选择 **Endoscopy > Build Quest APK**。脚本配置Android、ARM64、IL2CPP、OpenXR和主场景，输出 **D:\quest3\EndoscopyBuild\builds\EndoscopyInspection-Quest3.apk**。
4. 直接安装并启动：USB连接Quest并允许USB调试，在 **File > Build Profiles > Android > Run Device** 中刷新并选择Quest，然后点击 **Endoscopy > Build And Run on Quest**。此操作会在编译后安装并发送启动请求；进入VR时需佩戴头显、开启手部追踪并让头显看到双手。
5. 首次切换Android可能需要等待资源重新导入。若Unity提示目标切换后需要重试，等待编译完成再点击上述菜单。

也可以关闭本项目Unity编辑器后，双击 **D:\quest3\Build-Quest-APK.cmd**，独立编译APK。详细日志在 **artifacts/quest.log**。批处理与Unity编辑器不能同时构建同一个工程。

Unity原生Build Profiles中的Build/Build And Run也可以使用：确认Android已激活、Scene List包含并勾选Inspection场景、Build App Bundle和Export Project关闭，输出路径选择英文入口下的builds目录。

## 当前源码：0.8.0 手部动作教学

开启 Quest 系统手部追踪，放下手柄，将双手放到头显前。默认进入第一关，按短提示触碰门边亮点，再观察右侧隔断。

面板只能用食指近距离触碰。可拿资料在下端标出捏取点：拇指和食指先张开再合拢，拿近查看，放入指定绿色框后张开两指。包装样例需要翻到背面。放错、未查看、追踪丢失不会计为完成。完成后才显示继续按钮。

全程静音，包括教程、按钮和环境音。站内真实走动、转身；跨关需站稳后主动触碰换站按钮，无摇杆或键盘移动。长产品原图按需打开，案例判断放在动作完成后的可选复习入口。

当前源码版本为 0.8.0 / code 9；本轮没有编译 APK。下方现存安装包仍是 0.7.0，直接安装旧文件不会包含本轮改动。操作详情见[手部动作版本说明](docs/HANDS_ON_RELEASE.md)。

## 安装包和自行安装

APK：`D:\quest3\EndoscopyBuild\builds\EndoscopyInspection-Quest3.apk`

版本 0.7.0，versionCode=8，包名 com.endoscopy.inspection，覆盖旧版安装。连接 Quest 并允许 USB 调试后，在 PowerShell 运行：

```powershell
& 'D:\quest3\EndoscopyBuild\tools\install-quest.ps1' -Launch
```

或运行：

```powershell
adb install -r "D:\quest3\EndoscopyBuild\builds\EndoscopyInspection-Quest3.apk"
```

## 回去后优先测试

1. 不拿手柄能否进入，左右手是否随手指弯曲；不触碰时不会自动推进。
2. 食指接近、触碰、收回是否各触发一次；鼠标/手柄不应代替触碰。
3. 在资料下端两指捏取，拿近查看并放入对应框；放错、未查看不能通过。
4. 包装资料正面字是否正常，翻转后背面字是否正常，是否没有镜像重叠。
5. 手离开追踪范围时资料是否回原处，重新出现后是否无误触。
6. 记录行、标签放大、继续按钮是否清楚可达，真实走动和主动换站是否舒适。
7. 教程、交互、切换与复习是否全程无声；六关是否可完成并显示记录。

本版未做佩戴验证。编辑器动作测试使用模拟姿态，不能证明 Quest 实际手部识别、双眼可读性和帧率。专业特写仍有缺口，当前动作教学不等于甲方完整内容已验收。
